---
track_type: 咨询类
sub_type: 接口用法咨询
product: IM-SDK
feature: 服务端API
updated: 2025-04-30
---

<!-- entry: platform=Server, created=2025-06-03, customers=["一泓福瑞文化产业有限公司"], source=chat_history, frequency=1, tags=["attachment","群通知","ope","服务端API"] -->
## Q: 群通知消息attachment字段解析

**问题描述**：客户查询服务端历史消息接口，messageType为5的通知消息attachment字段内容根据ope类型不同而变化，需要了解各字段含义。

**答案**：ope字段表示操作类型：0-邀请入群，1-被邀请人同意，3-群信息更新。joinmode表示入群模式。

---

<!-- entry: platform=服务端, created=2025-02-07, customers=["杭州祺鲸"], source=chat_history, frequency=1, tags=["IM-SDK", "token", "服务端API", "隐私数据"] -->
## Q: IM服务端无法查询和刷新用户token

**问题描述**：用户询问能否查询云信侧accid对应的token，或要求直接更新token。

**答案**：用户需自行调用服务端API刷新token，客服可用demo验证登录或查询服务器登录302日志

<!-- entry: platform=服务端, created=2025-05-23, customers=["南京芷间科技有限公司"], source=chat_history, frequency=1, tags=["批量发送", "msgids", "returnMsgid", "服务端API"] -->
## Q: 批量发送单聊消息接口返回值没有msgids

**问题描述**：调用批量发送单聊消息接口，返回值中没有msgids字段。

**答案**：需要在请求参数中添加returnMsgid字段并设置为true，才会返回msgids。

---

<!-- entry: platform=服务端, created=2025-05-13, customers=["广州触海网络科技"], source=chat_history, frequency=1, tags=["OpenAPI", "HTTP1.1", "KeepAlive", "服务端"] -->
## Q: 服务端OpenAPI支持HTTP1.1和KeepAlive

**问题描述**：客户咨询服务端OpenAPI是否支持HTTP1.1和KeepAlive。

**答案**：服务端支持HTTPS 1.1和KeepAlive，参考文档https://doc.yunxin.163.com/messaging2/server-apis

---

<!-- entry: platform=Server, created=2025-04-01, customers=["江苏三六五网络"], source=chat_history, frequency=1, tags=["V10", "messageType", "消息类型", "枚举"] -->
## Q: V10消息体messageType字段枚举值说明

**问题描述**：## 问题：V10消息体messageType字段枚举值说明 ## 问题详情 **现象**： 客户咨询V10消息体中messageType字段的含义，需要消息类型枚举说明文档。 **环境信息**： - 平台：服务端 ## 解决方案 messageType字段枚举值：0-文本消息，1-图片消息，2-语音消息，3-视频消息，4-地理位置消息，6-文件消息，10-

**答案**：messageType字段枚举值：0-文本消息，1-图片消息，2-语音消息，3-视频消息，4-地理位置消息，6-文件消息，10-提示消息，100-自定义消息

---

<!-- entry: platform=Server, created=2025-04-01, customers=["时间在线"], source=chat_history, frequency=1, tags=["querySessionMsg", "limit", "分页查询", "服务端API", "历史消息"] -->
## Q: querySessionMsg接口limit参数使用问题

**问题描述**：## 问题：querySessionMsg接口limit参数使用问题 ## 问题详情 **现象**： 调用服务端querySessionMsg接口查询历史消息时，limit传1没有数据返回，传2才有数据。 **环境信息**： - 平台：服务端 ## 排查过程 1. 客户反馈limit=1时返回空数组，limit=2时有数据 2. 检查时间戳参数设置 3. 建

**答案**：使用分页查询时，可以先传begintime为0，然后以返回的最早消息的时间戳作为下一次查询的endtime继续往前遍历查询。

---

<!-- entry: platform=Server, created=2025-04-01, customers=["深圳富旅奇缘"], source=chat_history, frequency=1, tags=["聊天室", "队列", "operator", "服务端API", "文档"] -->
## Q: 更新聊天室队列operator参数说明

**问题描述**：## 问题：更新聊天室队列operator参数说明 ## 问题详情 **现象**： 咨询更新聊天室队列接口的operator参数是否影响修改操作。 **环境信息**： - 平台：服务端 ## 排查过程 1. 客户咨询operator不传是否影响修改 2. 客户传了不在线的账号提示错误 3. 文档描述有歧义 4. 确认不能传不在线的非创建者 ## 根因分析 文

**答案**：operator参数不能传不在线的非创建者。如果不传可以正常修改队列，传了只会影响添加元素成功后的通知事件中的操作者显示。

---
