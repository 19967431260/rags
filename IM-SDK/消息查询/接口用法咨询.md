---
track_type: 咨询类
sub_type: 接口用法咨询
product: IM-SDK
feature: 消息查询
updated: 2026-03-25
---

<!-- entry: platform=Android, created=2026-02-02, customers=["搜狐"], source=chat_history, frequency=1, tags=["queryMessageListByUuid", "queryQuickComment", "快捷评论", "消息查询"] -->
## Q: 通过消息UUID查询快捷评论

**问题描述**：咨询除了通过原始消息对象查询快捷评论外，是否可以通过消息ID查询。

**答案**：1. 可以使用queryMessageListByUuid通过UUID查询消息对象。2. queryQuickComment只能通过传入消息对象的方式查询，没有其他API支持直接通过ID查询。

---

<!-- entry: platform=iOS, created=2025-12-02, customers=["FVIP-云信-上海抱朴"], source=chat_history, frequency=1, tags=["iOS", "IM V9", "serverId", "NIMMessage", "消息查询"] -->
## Q: iOS IM SDK V9如何根据消息的serverId查找对应的NIMMessage对象？

**问题描述**：客户需要根据消息的serverId查找对应的NIMMessage对象，但不清楚V9中如何查询。

**答案**：V9中不能直接用serverId查询本地消息，需要通过客户端messageId查询。如果只有serverId，可以使用云端消息获取补偿机制（fetchMessageHistory）。

参考文档：https://doc.yunxin.163.com/messaging/guide/TQ0OTI4MDY?platform=iOS

---

<!-- entry: platform=通用, created=2025-02-10, customers=["成都自由泳科技有限公司"], source=chat_history, frequency=1, tags=["IM-SDK", "消息查询", "数据不存在", "聊天记录"] -->
## Q: 消息收发记录查询功能说明

**问题描述**：客户查询后台消息收发记录显示数据不存在，误以为是自己业务功能的问题。实际该功能是查询两个用户之间的聊天历史消息，如果未聊过天或消息不存云端则查不到。

**答案**：消息收发记录查询仅针对已发生的聊天消息，未聊过天或消息不存云端时显示数据不存在

<!-- entry: platform=iOS, created=2025-05-13, customers=["南京芷间科技有限公司"], source=chat_history, frequency=1, tags=["消息ID", "查询", "消息体", "历史记录"] -->
## Q: 根据消息ID获取消息体的方法

**问题描述**：需要实现关键词历史记录跳转，发现锚点是一个消息体，需要获取消息体。

**答案**：使用批量查询指定历史消息列表接口，根据消息ID查询消息详情。

---

<!-- entry: platform=Web, created=2025-05-16, customers=["山西云钢智造网络科技"], source=chat_history, frequency=1, tags=["getMessageList", "分页", "未读数", "Web", "anchorMessage"] -->
## Q: getMessageList分页查询和未读数获取

**问题描述**：咨询Web端getMessageList方法分页查询参数说明，以及如何获取未读历史消息总数。

**答案**：getMessageList返回一组消息，利用limit参数实现分批次获取。分页方式：每次获取固定条数（如10条），用户往上拉再获取前10条，第二次查询的anchorMessage是前一次请求返回的最后一条消息。未读数获取：云端会话用getTotalUnreadCount，本地会话用对应接口。

---
