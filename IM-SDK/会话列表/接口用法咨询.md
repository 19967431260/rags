---
track_type: 咨询类
sub_type: 接口用法咨询
product: IM-SDK
feature: 会话列表
updated: 2026-03-17
---

<!-- entry: created=2026-01-16, customers=["北京宝宝树"], source=chat_history, frequency=2, tags=["fromclienttype", "鸿蒙", "HARMONY", "平台标识"] -->
## Q: fromclienttype字段值含义-鸿蒙平台标识

**问题描述**：服务端会话列表查询接口返回的fromclienttype字段值为65，不清楚含义。

**答案**：fromclienttype字段值含义：1=Android、2=iOS、4=PC、16=WEB、32=REST(服务端RESTApi)、64=MAC、65=HARMONY(纯鸿蒙5/6)。

---

<!-- entry: platform=iOS, created=2026-02-05, customers=["常州和印网络科技有限公司"], source=chat_history, frequency=1, tags=["未读数", "标记已读", "会话列表", "iOS", "clearUnreadCount"] -->
## Q: 如何清除会话未读数

**问题描述**：用户咨询在iOS端打开会话消息后如何标记已读，使下次收到消息时未读数从1开始计数。需要清理会话未读数的API接口，单聊和群组都有此需求。

**答案**：调用清理会话未读数接口，SDK只支持直接清零未读数，无法清理到指定数值。参考文档：https://doc.yunxin.163.com/messaging/guide/TE0NzkwMzE?platform=iOS#未读数多端同步

---

<!-- entry: created=2026-02-10, customers=["FVIP云信-时间在线-gj"], source=chat_history, frequency=1, tags=["Web", "updateTime", "会话列表", "time字段"] -->
## Q: 会话updateTime字段赋值问题

**问题描述**：客户询问是否可以手动将消息的time字段赋值给会话的updateTime字段。SDK代码中也是从lastMsg.time取值。

**答案**：可以手动将消息的time赋值给updateTime，一般情况下time字段是准确的。建议测试赋值后是否有其他问题。

---

<!-- entry: platform=Android, created=2026-02-03, customers=["极之客"], source=chat_history, frequency=1, tags=["clearChattingHistory", "会话列表", "删除消息", "IM V9", "Android"] -->
## Q: 清除聊天记录后如何保留空会话

**问题描述**：调用clearChattingHistory清除历史聊天记录后,希望保留空会话在会话列表中,不删除会话本身。客户询问是否有删除消息的监听方法来同步更新会话列表。

**答案**：直接调用clearChattingHistory即可,该接口只删除会话内的本地消息数据,不会删除会话本身。清空后会话最后一条消息为null,通过getRecentMessageId查询返回null表示本地消息已全部清空。删除消息没有监听回调。参考:https://doc.yunxin.163.com/messaging/guide/TgyOTE3MDI?platform=android

---

<!-- entry: platform=iOS, created=2026-02-03, customers=["浙江驿乐网络科技有限公司"], source=chat_history, frequency=1, tags=["未读消息", "会话", "群聊", "iOS", "V9"] -->
## Q: iOS V9获取群聊未读消息数量

**问题描述**：咨询iOS V9版本如何获取某个群的未读消息数量，以及未加入的群聊是否能获取未读消息数量。

**答案**：未读数是会话维度的，使用群id查询会话即可获取。未加入的群聊无法获取未读消息数量。

---

<!-- entry: platform=Android, created=2026-02-05, customers=["WooPlus"], source=chat_history, frequency=1, tags=["云端会话","onConversationChanged","离线消息","数据同步"] -->
## Q: 登录同步离线消息不触发云端会话变更回调

**问题描述**：Android登录后完成数据同步,同步离线消息到本地,只触发localConversationService的onConversationChanged,不触发conversationService的onConversationChanged,导致使用云端会话的业务层会话未读数未更新

**答案**：需要在收到数据同步结束后业务层重新刷新会话列表。本地会话和云端会话的未读数是独立计算的,清零接口也是分别独立的

---
