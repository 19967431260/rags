---
track_type: 排查类
sub_type: 线上事故
product: IM-SDK
feature: 群组
platform: 服务端
title: 创建群组返回806错误-用户群组数量超限
root_cause: 用户加入的群组数量超过5000个上限
solution: 通过服务端API拉取用户进群列表，使用remove接口强制退出部分群组释放额度
customers: ["杭州徕檬"]
source: chat_history
tags: ["806", "创建群组", "群组上限", "生产事故"]
created: 2025-11-26
updated: 2026-03-20
---

## 问题：服务端 创建群组返回806错误-用户群组数量超限

## 问题详情

**现象**：
生产环境紧急问题：三个用户请求创建群组必定失败，返回错误码806。经排查发现用户加入的群组数量已达上限。

**复现步骤**：
（从会话提取；无明确步骤则省略此节）

**环境信息**：
- SDK 版本：（从会话提取，无则省略）
- 系统版本 / 设备：（从会话提取，无则省略）
- 其他：（网络环境、账号类型等，无则省略）

**相关日志**：
（从会话提取关键报错日志，无则省略）

## 排查过程

1. 客户反馈创建群返回806错误
2. 技术支持查询发现用户加入群组数量超限
3. 每个用户最多加入5000个群

**关键发现**：

## 问题原因

用户加入的群组数量超过5000个上限

## 解决方案

通过服务端API拉取用户进群列表，使用remove接口强制退出部分群组释放额度

## 其他触发场景

