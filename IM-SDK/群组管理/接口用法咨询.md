---
track_type: 咨询类
sub_type: 接口用法咨询
product: IM-SDK
feature: 群组管理
updated: 2026-03-23
---

<!-- entry: platform=iOS, created=2025-11-21, customers=["VIP云信-重庆英纳华科技有限公司"], source=chat_history, frequency=1, tags=["禁言状态", "ismuted", "teamMember", "iOS"] -->
## Q: 获取群成员禁言状态的方法

**问题描述**：需要获取群成员是否被禁言的状态。

**答案**：调用teamManager的teamMember方法获取指定成员信息，返回对象的ismuted属性可判断是否被禁言。

---
<!-- entry: platform=iOS, created=2025-11-27, customers=["VIP云信-重庆英纳华科技有限公司"], source=chat_history, frequency=1, tags=["allMuteMode", "群禁言", "查询", "iOS"] -->
## Q: 查询群组禁言状态的方法

**问题描述**：需要查询当前群的禁言状态。

**答案**：通过teamManager的teamById获取NIMTeam对象，使用allMuteMode属性查询群禁言状态。

---
<!-- entry: platform=Server, created=2026-02-02, customers=["启明星云数据技术有限公司"], source=chat_history, frequency=1, tags=["服务端", "群组", "管理员", "角色管理"] -->
## Q: 服务端如何更新群成员角色

**问题描述**：客户端SDK有updateTeamMemberRole接口可以更新群成员角色，咨询服务端是否有对应接口。

**答案**：服务端有添加群管理员和移除群管理员接口。如果一个群成员已经是管理员，再调用接口添加他为管理员会报错。

---
<!-- entry: platform=Server, created=2026-02-04, customers=["启明星云数据技术有限公司"], source=chat_history, frequency=1, tags=["服务端", "群组", "群昵称", "team_nick"] -->
## Q: 邀请入群时群昵称的默认值规则

**问题描述**：调用邀请入群接口拉账号到群里，如果没有显性指定群昵称，通过拉取群成员列表接口返回的team_nick字段为空。

**答案**：team_nick字段只有通过接口显性指定了才会有值，否则为空。如果使用UIKit会自动取用户资料展示，如果没有使用UIKit需要自行取用户资料展示。

---
<!-- entry: platform=Android, created=2026-02-04, customers=["广州豆豆信息科技有限公司"], source=chat_history, frequency=1, tags=["群扩展字段", "custom", "queryTeam", "getExtension", "群信息更新"] -->
## Q: 服务端群扩展字段custom客户端获取方法

**问题描述**：服务端创建高级群时设置了custom扩展字段，客户端需要获取该字段的值。

**答案**：调用客户端的queryTeam接口，返回的team对象有getExtension()方法可以直接获取custom字段。监听群信息更新可以通过监听通知消息，notificationtype为updateteam时表示群信息变更。

---
<!-- entry: platform=Server, created=2026-02-09, customers=["启明星云数据技术有限公司"], source=chat_history, frequency=1, tags=["服务端", "客户端", "免打扰", "参数", "文档"] -->
## Q: 客户端和服务端群消息免打扰参数值相反

**问题描述**：群消息免打扰设置，客户端参数和服务端参数不一样，免打扰开启或关闭两端的值是相反的。

**答案**：客户端和服务端的免打扰参数值确实是相反的，需要注意区分。通过服务端接口设置为1，在客户端拿到的实际是0。免打扰分3个状态：1.接收所有消息提醒 2.只接收群主和管理员的普通消息且接收所有@消息 3.不接收所有人发的普通消息仅接收@消息。@消息是强提醒无法屏蔽。

---
<!-- entry: platform=通用, created=2026-02-12, customers=["福州市玄星网络科技有限公司"], source=chat_history, frequency=1, tags=["群组", "入群申请", "getTeamJoinActionInfoList", "Flutter"] -->
## Q: 获取入群申请列表

**问题描述**：群管理员需要获取申请加入群的列表。

**答案**：调用getTeamJoinActionInfoList方法分页获取入群申请列表。返回列表按照入群请求时间戳（V2NIMTeamJoinActionInfo.timestamp）逆序排序。

---
<!-- entry: platform=通用, created=2026-02-25, customers=["VIP云信-四川布谷鸟信息技术有限公司"], source=chat_history, frequency=1, tags=["群组", "创建群组", "群主", "服务端API"] -->
## Q: 创建群组时如何设置群主

**问题描述**：客户询问使用服务端API创建群组时如何指定群主。

**答案**：服务端创建群组接口中，owner字段用于指定群主accid。如果不传owner字段，则默认创建者为群主。

---
<!-- entry: platform=Server, created=2025-06-12, customers=["海康华安"], source=chat_history, frequency=1, tags=["高级群", "joinMode", "入群验证", "invitemode"] -->
## Q: 高级群入群验证模式joinMode说明

**问题描述**：设置joinMode需要管理员或群主同意后加入，但普通成员拉人进群时仍不用审批

**答案**：joinMode指群外账号申请入群是否需要审批，群内成员主动拉人入群不受此限制；joinMode只控制群外申请入群；要实现群内拉人审批，需配置invitemode为0，由管理员/群主拉人，业务层自行实现申请同意逻辑

---
