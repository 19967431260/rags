---
track_type: 排查类
sub_type: 使用问题
product: IM-SDK
feature: 会话管理
platform: 通用
title: 创建会话报403是因为调用了云端会话接口
root_cause: 创建会话报403是因为调用了云端会话接口但未开通对应功能
solution: 在云信控制台开通云端会话功能，或确认接口调用的正确性。
customers: ["惠州市拓墟互联网有限公司"]
source: chat_history
tags: ["创建会话", "403", "云端会话"]
created: 2025-08-18
updated: 2026-03-26
---

## 问题：通用 创建会话报403是因为调用了云端会话接口

## 问题详情

**现象**：
创建会话报403错误。

**复现步骤**：
（从会话提取；无明确步骤则省略此节）

**环境信息**：
- SDK 版本：（从会话提取，无则省略）
- 系统版本 / 设备：（从会话提取，无则省略）
- 其他：（网络环境、账号类型等，无则省略）

**相关日志**：
HTTP 403错误

## 排查过程

**关键发现**：创建会话报403是因为调用了云端会话接口但未开通对应功能

## 问题原因

创建会话报403是因为调用了云端会话接口但未开通对应功能

## 解决方案

在云信控制台开通云端会话功能，或确认接口调用的正确性。

## 其他触发场景

（合并时在此处追加：`- [{platform}] {title}，来源：{customers}，{today}`）
