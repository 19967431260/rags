---
track_type: 排查类
sub_type: 配置开通咨询
product: IM-SDK
feature: 推送
platform: iOS
title: iOS推送证书配置问题
root_cause: 客户端配置的推送证书名不正确，使用了默认的your apnsCername。
solution: 1. 配置正确的APNs证书名；2. 根据打包环境（开发/生产）选择对应证书；3. 正式证书在开发环境也可用，测试证书只能在测试环境使用。
customers: ["VIP云信-四川斯特雅科技有限责任公司"]
source: chat_history
tags: ["iOS推送", "APNs", "证书", "Flutter"]
created: 2025-11-18
updated: 2026-03-20
---

## 问题：iOS iOS推送证书配置问题

## 问题详情

**现象**：
iOS推送没有反应，客户端配置的推送证书名显示为your apnsCername。

**复现步骤**：
（从会话提取；无明确步骤则省略此节）

**环境信息**：
- SDK 版本：（从会话提取，无则省略）
- 系统版本 / 设备：（从会话提取，无则省略）
- 其他：（网络环境、账号类型等，无则省略）

**相关日志**：
（从会话提取关键报错日志，无则省略）

## 排查过程

1. 检查推送token是否上报；2. 确认推送证书名配置。

**关键发现**：

## 问题原因

客户端配置的推送证书名不正确，使用了默认的your apnsCername。

## 解决方案

1. 配置正确的APNs证书名；2. 根据打包环境（开发/生产）选择对应证书；3. 正式证书在开发环境也可用，测试证书只能在测试环境使用。

## 其他触发场景

