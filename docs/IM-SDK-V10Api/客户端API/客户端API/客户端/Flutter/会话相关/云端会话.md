网易云信即时通讯 SDK（简称 NIM SDK）支持云端会话管理功能，包括创建、更新、删除云端会话等基础操作，以及置顶会话等进阶操作。

如何实现云端会话相关功能请参考 [云端会话管理](https://doc.yunxin.163.com/messaging2/client-apis/zExMDk4ODU?platform=client)。本文介绍 NIM SDK 云端会话相关 API。

## API 概览

### 云端会话监听

API | 说明 | 起始版本
---- | ---- | ----
[listen](#listen) | 注册云端会话相关监听器 | v10.3.0
[cancel](#cancel) | 取消注册云端会话相关监听器 | v10.3.0


### 会话操作

API | 说明 | 起始版本
---- | ---- | ----
[createConversation](#createConversation) | 创建一条空会话 | v10.3.0
[deleteConversation](#deleteConversation) | 删除一条会话 | v10.3.0
[deleteConversationListByIds](#deleteConversationListByIds) | 批量删除会话列表 | v10.3.0
[getConversation](#getConversation) | 获取单条会话 | v10.3.0
[getConversationList](#getConversationList) | 获取会话列表 | v10.3.0
[getConversationListByOption](#getConversationListByOption) | 获取指定会话列表 | v10.3.0
[getConversationListByIds](#getConversationListByIds) | 根据会话 ID 批量获取会话列表 | v10.3.0
[updateConversation](#updateConversation) | 更新会话服务端扩展字段 | v10.3.0
[updateConversationLocalExtension](#updateConversationLocalExtension) | 更新会话本地扩展字段 | v10.3.0
[stickTopConversation](#stickTopConversation) | 置顶会话 | v10.3.0
[getStickTopConversationList](#getStickTopConversationList) | 查询当前置顶的全量云端会话列表 | v10.9.0

### 会话未读数

API | 说明 | 起始版本
---- | ---- | ----
[getTotalUnreadCount](#getTotalUnreadCount) | 获取全部会话的消息总未读数 | v10.3.0
[getUnreadCountByIds](#getUnreadCountByIds) | 获取指定会话列表的消息总未读数 | v10.3.0
[getUnreadCountByFilter](#getUnreadCountByFilter) | 根据过滤参数获取相应的消息未读数 | v10.3.0
[clearTotalUnreadCount](#clearTotalUnreadCount) | 清空所有会话总的未读数 | v10.3.0
[clearUnreadCountByIds](#clearUnreadCountByIds) | 清空指定会话列表的消息总未读数 | v10.3.0
[clearUnreadCountByTypes](#clearUnreadCountByTypes)| 根据会话类型清除指定会话类型的所有未读数|v10.3.0
[subscribeUnreadCountByFilter](#subscribeUnreadCountByFilter) | 订阅过滤后的会话未读数变化 | v10.3.0
[unsubscribeUnreadCountByFilter](#unsubscribeUnreadCountByFilter) | 取消订阅过滤后的会话未读数变化 | v10.3.0
[markConversationRead](#markConversationRead) | 标记会话已读时间戳 | v10.3.0
[getConversationReadTime](#getConversationReadTime) | 获取会话已读时间戳 | v10.3.0
[clearUnreadCountByGroupId](#clearUnreadCountByGroupId)|根据会话分组 ID 清除分组内所有会话的消息总未读数 | v10.8.0

## 接口类

`ConversationService` 类提供创建、删除、更新、获取云端会话，云端会话未读数、置顶会话、注册云端会话监听等接口。

## <span id="listen">listen</span>

**接口描述**

注册云端会话相关监听。

注册成功后，当事件发生时，SDK 会返回对应的回调。

::: note note
- 建议在初始化后调用该方法。
- 全局只需注册一次。
- 该接口为同步。
:::

**回调事件**

- `onSyncStarted`：数据同步开始。
- `onSyncFinished`：数据同步结束回调。
- `onSyncFailed`：数据同步失败回调，返回 [相关错误码](https://doc.yunxin.163.com/messaging2/client-apis/DUxNjU3MzU?platform=client)。
- `onConversationCreated`：会话创建回调，返回创建的会话对象。
- `onConversationDeleted`：会话删除回调，返回被删除的会话 ID 列表。
- `onConversationChanged`：会话变更回调，返回变更后的会话列表。
- `onTotalUnreadCountChanged`：会话消息总未读数变更，返回变更后的总未读数。
- `onUnreadCountChangedByFilter`：指定过滤条件的会话消息未读数变更，根据过滤条件返回变更后的未读数。
- `onConversationReadTimeUpdated`：同一账号多端登录后的会话已读时间戳标记的时间变更，返回同步标记的会话 ID 和标记的时间戳。

**示例代码**

```dart
subsriptions.add(NimCore.instance.conversationService.onSyncStarted.listen((e) {
      //do something
    }));
    subsriptions.add(NimCore.instance.conversationService.onSyncFinished.listen((e) {
      //do something
    }));
    subsriptions.add(NimCore.instance.conversationService.onSyncFailed.listen((e) {
      //do something
    }));
    subsriptions.add(NimCore.instance.conversationService.onConversationCreated.listen((e) {
      //do something
    }));
    subsriptions.add(NimCore.instance.conversationService.onConversationDeleted.listen((e) {
      //do something
    }));
    subsriptions.add(NimCore.instance.conversationService.onConversationChanged.listen((e) {
      //do something
    }));
    subsriptions.add(NimCore.instance.conversationService.onTotalUnreadCountChanged.listen((e) {
      //do something
    }));
    subsriptions.add(NimCore.instance.conversationService.onUnreadCountChangedByFilter.listen((e) {
      //do something
    }));
    subsriptions.add(NimCore.instance.conversationService.onConversationReadTimeUpdated.listen((e) {
      //do something
    }));
```

## <span id="cancel">cancel</span>

**接口描述**

取消注册云端会话相关监听。

::: note note
该接口为同步。
:::

**示例代码**

```dart
subsriptions.forEach((subsription) {
      subsription.cancel();
    });
```

## <span id="createConversation">createConversation</span>

**接口描述**

创建一条空会话。

创建成功后，SDK 会返回创建成功回调 `onConversationCreated`，并同步缓存和数据库。

::: note note
如果没有消息收发，该会话仅创建人可见。
:::

**适用场景**

当需要占位空会话时可以调用该方法，一般情况下无需主动调用，收发消息时 SDK 会自动创建会话。

**参数说明**

```dart
Future<NIMResult<NIMConversation>> createConversation(
    String conversationId) async {
  return _platform.createConversation(conversationId);
}
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationId` | String | 是 | 会话 ID，通过调用 `ConversationIdUtil` 的对应函数创建。<li>组成方式：用户账号（accountId）\| 会话类型（`NIMConversationType`）\| 聊天对象账号（accountId）或群组 ID。</li><li>不可设置为空，否则返回 191004 参数错误。</li> |

**示例代码**

```dart
await NimCore.instance.conversationService.createConversation(conversationId)
```

**返回值**

NIMResult\<NIMConversation>：会话相关信息

**相关回调**

`onConversationCreated`：会话创建回调

## <span id="deleteConversation">deleteConversation</span>

**接口描述**

删除指定会话。

删除成功后，SDK 会返回删除成功回调 `onConversationDeleted`，并同步数据库和缓存。

如果被删除会话中有消息未读，SDK 还会返回 `onTotalUnreadCountChanged` 和 `onUnreadCountChangedByFilter` 回调。

**参数说明**

```dart
Future<NIMResult<void>> deleteConversation(
    String conversationId, bool clearMessage) async {
  return _platform.deleteConversation(conversationId, clearMessage);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationId` | String | 是 | 会话 ID，如果为空、不合法、不存在则返回 191004 参数错误。 |
| `clearMessage` | bool | 否 | 是否同步删除会话对应的历史消息，默认 false。 <li>true：删除历史消息。</li><li>false：只删除会话，保留历史消息。</li> |

**示例代码**

```dart
await NimCore.instance.conversationService.deleteConversation(conversationId, clearMessage)
```

**返回值**

NIMResult\<void>

**相关回调**

- `onConversationDeleted`：会话删除回调 
- `onTotalUnreadCountChanged`：会话消息总未读数变更回调
- `onUnreadCountChangedByFilter`：指定过滤条件的会话消息未读数变更回调

## <span id="deleteConversationListByIds">deleteConversationListByIds</span>

**接口描述**

批量删除指定的会话列表。

删除每一条会话成功后，SDK 均会返回删除成功回调 `onConversationDeleted` 回调，并同步数据库和缓存。

如果被删除会话中有消息未读，SDK 还会返回 `onTotalUnreadCountChanged` 和 `onUnreadCountChangedByFilter` 回调。

**参数说明**

```dart
Future<NIMResult<List<NIMConversationOperationResult>>>
    deleteConversationListByIds(
        List<String> conversationIds, bool clearMessage) async {
  return _platform.deleteConversationListByIds(conversationIds, clearMessage);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationIds` | List\<String> | 是 | 会话 ID 列表。<li>不可为空，否则返回 191004 参数错误。<li>单次调用上限为 100 条。 |
| `clearMessage` | bool | 否 | 是否同步删除会话对应的历史消息，默认为 false。<li>true：删除历史消息。</li><li>false：只删除会话，保留历史消息。</li> |

**示例代码**

```dart
await NimCore.instance.conversationService.deleteConversationListByIds(conversationIds, clearMessage)
```

**返回值**

NIMResult\<List\<NIMConversationOperationResult>>：单条云端会话操作结果

**相关回调**

- `onConversationDeleted`：会话删除回调
- `onTotalUnreadCountChanged` ：会话消息总未读数变更回调
- `onUnreadCountChangedByFilter` ：指定过滤条件的会话消息未读数变更回调

## <span id="getConversation">getConversation</span>

**接口描述**

查询单条指定会话。

::: note note
- 查询前请确保指定的会话存在。
- 数据同步完成前，可能查询不到完整数据。
:::

**参数说明**

```dart
Future<NIMResult<NIMConversation>> getConversation(
    String conversationId) async {
  return _platform.getConversation(conversationId);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationId` | String | 是 | 会话 ID，如果为空、不合法、不存在则返回 191004 参数错误。 |

**示例代码**

```dart
await NimCore.instance.conversationService.getConversation(conversationId)
```

**返回值**

NIMResult\<NIMConversation>：会话相关信息

**相关回调**

无

## <span id="getConversationList">getConversationList</span>

**接口描述**

分页查询所有会话列表。

该方法分页获取会话数据，直到获取全量会话。

::: note note
- 查询的会话列表结果中，置顶会话排首位。
- 数据同步完成前，可能查询不到完整数据。
:::

**参数说明**

```dart
Future<NIMResult<NIMConversationResult>> getConversationList(
    int offset, int limit) async {
  return _platform.getConversationList(offset, limit);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `offset` | int | 是 | 分页偏移。首次调用传 0，后续调用时传入上一次调用返回的 `offset`。 |
| `limit` | int | 是 | 单次查询会话数量上限，建议不超过 100。设置为小于或等于 0 则默认为 100。 |

**示例代码**

```dart
await NimCore.instance.conversationService.getConversationList(offset, limit)
```

**返回值**

NIMResult\<NIMConversationResult>：分页查询会话列表结果相关信息

**相关回调**

无

## <span id="getConversationListByOption">getConversationListByOption</span>

**接口描述**

根据指定条件分页查询会话列表。

该方法根据指定条件分页获取会话数据，直到获取全量会话，SDK 会进行数据同步并返回对应回调通知 UI 层。

::: note note
数据同步完成前，可能查询不到完整数据。
:::

**参数说明**

```dart
Future<NIMResult<NIMConversationResult>> getConversationListByOption(
    int offset, int limit, NIMConversationOption option) async {
  return _platform.getConversationListByOption(offset, limit, option);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `offset` | int | 是 | 分页偏移。首次调用传 0，后续调用时传入上一次调用返回的 `offset`。 |
| `limit` | int | 是 | 单次查询会话数量上限，建议不超过 100。设置为小于或等于 0 则默认为 100。 |
| `option` | [`NIMConversationOption`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client) | 是 | 查询条件，设置为空则查询所有会话列表。 |

**示例代码**

```dart
await NimCore.instance.conversationService.getConversationListByOption(offset, limit, option)
```

**返回值**

NIMResult\<NIMConversationResult>：分页查询会话列表结果相关信息

**相关回调**

无

## <span id="getConversationListByIds">getConversationListByIds</span>

**接口描述**

批量查询指定的会话列表。

::: note note
数据同步完成前，可能查询不到完整数据。
:::

**参数说明**

```dart
Future<NIMResult<List<NIMConversation>>> getConversationListByIds(
    List<String> conversationIds) async {
  return _platform.getConversationListByIds(conversationIds);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationIds` | List\<String> | 是 | 会话 ID 列表，不可为空，否则返回 191004 参数错误。 |

**示例代码**

```dart
await NimCore.instance.conversationService.getConversationListByIds(conversationIds)
```

**返回值**

NIMResult\<List\<NIMConversation>>：会话列表相关信息

**相关回调**

无

## <span id="updateConversation">updateConversation</span>

**接口描述**

更新指定会话的服务端扩展字段。

更新成功后，SDK 会返回会话变更回调 `onConversationChanged`，并同步数据库、缓存及本地扩展字段。

::: note notice
更新前请确保该会话已存在。
:::

**参数说明**

```dart
Future<NIMResult<void>> updateConversation(
    String conversationId, NIMConversationUpdate updateInfo) async {
  return _platform.updateConversation(conversationId, updateInfo);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationId` | String | 是 | 会话 ID，如果为空、不合法、不存在则返回 191004 参数错误。 |
| `updateInfo` | [`NIMConversationUpdate`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client) | 是 | 会话更新对象，可以更新服务端扩展字段，会多端同步。|

**示例代码**

```dart
await NimCore.instance.conversationService.updateConversation(conversationId, updateInfo)
```

**返回值**

NIMResult\<void>

**相关回调**

`onConversationChanged`：会话变更回调

## <span id="updateConversationLocalExtension">updateConversationLocalExtension</span>

**接口描述**

更新指定会话的本地扩展字段。

本地扩展字段更新后，SDK 会返回会话变更回调 `onConversationChanged`。

::: note notice
更新前请确保该会话已存在。
:::

**参数说明**

```dart
Future<NIMResult<void>> updateConversationLocalExtension(
    String conversationId, String localExtension) async {
  return _platform.updateConversationLocalExtension(
      conversationId, localExtension);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationId` | String | 是 | 会话 ID，如果为空、不合法、不存在则返回 191004 参数错误。 |
| `localExtension` | String| 是 | 更新会话本地扩展字段。|


**示例代码**

```dart
await NimCore.instance.conversationService.updateConversationLocalExtension(conversationId, localExtension)
```

**返回值**

NIMResult\<void>

**相关回调**

`onConversationChanged`：会话变更回调

## <span id="stickTopConversation">stickTopConversation</span>

**接口描述**

将会话置顶。最多支持置顶 100 条会话。

置顶成功后，SDK 会返回会话变更回调 `onConversationChanged`，并同步数据库和缓存。

::: note notice
置顶前请确保该会话已存在。
:::

**参数说明**

```dart
Future<NIMResult<void>> stickTopConversation(
    String conversationId, bool stickTop) async {
  return _platform.stickTopConversation(conversationId, stickTop);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationId` | String | 是 | 会话 ID，如果为空、不合法、不存在则返回 191004 参数错误。 |
| `stickTop` | bool | 是 | 是否置顶会话。 |

**示例代码**

```dart
await NimCore.instance.conversationService.stickTopConversation(conversationId, stickTop)
```

**返回值**

NIMResult\<void>

**相关回调**

`onConversationChanged`：会话变更回调

## <span id="getStickTopConversationList">getStickTopConversationList</span>

**接口描述**

查询当前全量置顶的云端会话列表。

**参数说明**

```dart
Future<NIMResult<List<NIMConversation>>> getStickTopConversationList()
```

**示例代码**

```dart
final result = await NimCore.instance.conversationService.getStickTopConversationList();
```

**返回值**

NIMResult\<List\<NIMConversation>> 置顶的云端会话列表

**相关回调**

无

## <span id="getTotalUnreadCount">getTotalUnreadCount</span>

**接口描述**

获取全部会话的消息总未读数。

::: note note
数据同步完成前，可能查询不到完整数据。
:::

**参数说明**

```dart
Future<NIMResult<int>> getTotalUnreadCount() async {
  return _platform.getTotalUnreadCount();
}
```

**示例代码**

```dart
await NimCore.instance.conversationService.getTotalUnreadCount()
```

**返回值**

NIMResult\<int>：会话消息总未读数

**相关回调**

无

## <span id="getUnreadCountByIds">getUnreadCountByIds</span>

**接口描述**

获取指定的会话列表的总未读数。

::: note note
- 查询前请确保指定的会话已存在。
- 数据同步完成前，可能查询不到完整数据。
:::

**参数说明**

```dart
Future<NIMResult<int>> getUnreadCountByIds(
    List<String> conversationIds) async {
  return _platform.getUnreadCountByIds(conversationIds);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationIds` | List\<String>| 是 | 会话 ID 列表，不可为空，否则返回 191004 参数错误。 |

**示例代码**

```dart
await NimCore.instance.conversationService.getUnreadCountByIds(conversationIds)
```

**返回值**

NIMResult\<int>：指定会话的消息总未读数

**相关回调**

无

## <span id="getUnreadCountByFilter">getUnreadCountByFilter</span>

**接口描述**

获取过滤后的所有会话列表消息总未读数。

::: note note
数据同步完成前，可能查询不到完整数据。
:::

**参数说明**

```dart
Future<NIMResult<int>> getUnreadCountByFilter(
    NIMConversationFilter filter) async {
  return _platform.getUnreadCountByFilter(filter);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `filter` | [`NIMConversationFilter`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client) | 是 | 过滤器配置，SDK 会按照该参数配置进行滤，并返回过滤后的会话列表的消息总未读数。 |

**示例代码**

```dart
await NimCore.instance.conversationService.getUnreadCountByFilter(filter)
```

**返回值**

NIMResult\<int>：过滤后的会话消息总未读数

**相关回调**

无

## <span id="clearTotalUnreadCount">clearTotalUnreadCount</span>

**接口描述**

将全部会话的消息总未读数清零。

清零成功后，SDK 返回 `onTotalUnreadCountChanged`、`onConversationChanged` 和 `onUnreadCountChangedByFilter` 回调，并同步数据库和缓存。

**参数说明**

```dart
Future<NIMResult<void>> clearTotalUnreadCount() async {
  return _platform.clearTotalUnreadCount();
}
```

**示例代码**

```dart
await NimCore.instance.conversationService.clearTotalUnreadCount()
```

**返回值**

NIMResult\<void>

**相关回调**

- `onTotalUnreadCountChanged`：会话消息总未读数变更回调
- `onConversationChanged` ：会话变更回调
- `onUnreadCountChangedByFilter`：指定过滤条件的会话消息未读数变更回调

## <span id="clearUnreadCountByIds">clearUnreadCountByIds</span>

**接口描述**

将指定会话列表的消息未读数清零。

清零成功后，SDK 返回 `onTotalUnreadCountChanged`、`onConversationChanged` 和 `onUnreadCountChangedByFilter` 回调，并同步数据库和缓存。

::: note notice
调用前请确保指定的会话已存在。
:::

**参数说明**

```dart
Future<NIMResult<List<NIMConversationOperationResult>>> clearUnreadCountByIds(
    List<String> conversationIds) async {
  return _platform.clearUnreadCountByIds(conversationIds);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationIds` | List\<String> | 是 | 会话 ID 列表。<li>不可为空，否则返回 191004 参数错误。<li>单次调用上限为 10 条。 |

**示例代码**

```dart
await NimCore.instance.conversationService.clearUnreadCountByIds(conversationIds)
```

**返回值**

NIMResult\<List\<NIMConversationOperationResult>>：单条云端会话操作结果

**相关回调**

- `onTotalUnreadCountChanged`：会话消息总未读数变更回调
- `onConversationChanged` ：会话变更回调
- `onUnreadCountChangedByFilter`：指定过滤条件的会话消息未读数变更回调

## <span id="clearUnreadCountByGroupId">clearUnreadCountByGroupId</span>

**接口描述**

将指定分组的会话消息未读数清零。

清零成功后，SDK 返回 `onTotalUnreadCountChanged`、`onConversationChanged` 和 `onUnreadCountChangedByFilter` 回调，并同步数据库和缓存。

::: note notice
调用前请确保指定的会话分组已存在。
:::

**参数说明**

```dart
Future<NIMResult<void>> clearUnreadCountByGroupId( String groupId )
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `groupId` | String | 是 | 会话分组 ID。通过调用 [`createConversationGroup`](https://doc.yunxin.163.com/messaging2/client-apis/zc2NDkxNjM?platform=client#createConversationGroup) 获得。不可为空，否则返回 191004 参数错误。 |

**示例代码**

```dart
await NimCore.instance.conversationService.clearUnreadCountByGroupId(groupId)
```

**返回值**

NIMResult\<void>

**相关回调**

- `onTotalUnreadCountChanged`：会话消息总未读数变更回调
- `onConversationChanged` ：会话变更回调
- `onUnreadCountChangedByFilter`：指定过滤条件的会话消息未读数变更回调

## <span id="clearUnreadCountByTypes">clearUnreadCountByTypes</span>

**接口描述**

将指定类型的会话消息未读数清零。

清零成功后，SDK 返回 `onTotalUnreadCountChanged`、`onConversationChanged` 和 `onUnreadCountChangedByFilter` 回调，并同步数据库和缓存。

**参数说明**

```dart
Future<NIMResult<void>> clearUnreadCountByTypes(
    List<NIMConversationType> conversationTypes) async {
  return _platform.clearUnreadCountByTypes(conversationTypes);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationTypes` | List\<[`NIMConversationType`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMConversationType)>| 是 | 会话类型。 |

**示例代码**

```dart
await NimCore.instance.conversationService.clearUnreadCountByTypes(conversationTypes)
```

**返回值**

NIMResult\<void>

**相关回调**

- `onTotalUnreadCountChanged`：会话消息总未读数变更回调
- `onConversationChanged` ：会话变更回调
- `onUnreadCountChangedByFilter`：指定过滤条件的会话消息未读数变更回调

## <span id="subscribeUnreadCountByFilter">subscribeUnreadCountByFilter</span>

**接口描述**

订阅过滤后的会话未读数变化监听。您可以多次调用该方法，订阅多个过滤器的会话未读数监听。

订阅成功后，当过滤后的会话未读数变化，SDK 返回 `onUnreadCountChangedByFilter` 回调。

**参数说明**

```dart
Future<NIMResult<NIMError>> subscribeUnreadCountByFilter(
    NIMConversationFilter filter) async {
  return _platform.subscribeUnreadCountByFilter(filter);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `filter` | [`NIMConversationFilter`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMConversationFilter) | 是 | 会话过滤对象<li>如果该对象参数均为空，则返回 191004 错误码。<li>如果该对象设置多个参数且生效，则返回参数对应的多个回调。如果设置多个参数但有参数错误未生效，则不触发任何回调。 |


**示例代码**

```dart
await NimCore.instance.conversationService.subscribeUnreadCountByFilter(filter)
```

**返回值**

NIMResult\<NIMError>

**相关回调**

`onUnreadCountChangedByFilter`：指定过滤条件的会话消息未读数变更回调

## <span id="unsubscribeUnreadCountByFilter">unsubscribeUnreadCountByFilter</span>

**接口描述**

取消订阅过滤后的会话未读数变化监听。

**参数说明**

```dart
Future<NIMResult<NIMError>> unsubscribeUnreadCountByFilter(
    NIMConversationFilter filter) async {
  return _platform.unsubscribeUnreadCountByFilter(filter);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `filter` | [`NIMConversationFilter`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMConversationFilter) | 是 | 会话过滤对象。如果该对象参数均为空，则返回 191004 错误码。 |

**示例代码**

```dart
await NimCore.instance.conversationService.unsubscribeUnreadCountByFilter(filter)
```

**返回值**

NIMResult\<NIMError>

**相关回调**

无

## <span id="markConversationRead">markConversationRead</span>

**接口描述**

标记会话已读时间戳。当前只支持 P2P，高级群，超大群会话类型。

标记成功后，SDK 会同步数据库和缓存。

**参数说明**

```dart
Future<NIMResult<int>> markConversationRead(String conversationId) async {
  return _platform.markConversationRead(conversationId);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationId` | String | 是 | 会话 ID，如果为空、不合法、不存在则返回 191004 参数错误。 |

**示例代码**

```dart
await NimCore.instance.conversationService.markConversationRead(conversationId)
```

**返回值**

NIMResult\<int>：标记的已读时间戳

**相关回调**

无

## <span id="getConversationReadTime">getConversationReadTime</span>

**接口描述**

获取指定会话的已读时间戳。

::: note note
- 查询前请确保指定的会话已存在。
- 数据同步完成前，可能查询不到完整数据。
:::

**参数说明**

```dart
Future<NIMResult<int>> getConversationReadTime(String conversationId) async {
  return _platform.getConversationReadTime(conversationId);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `conversationId` | String | 是 | 会话 ID，如果为空、不合法、不存在则返回 191004 参数错误。 |


**示例代码**

```dart
await NimCore.instance.conversationService.getConversationReadTime(conversationId)
```

**返回值**

NIMResult\<int>：指定会话的已读时间戳

**相关回调**

无