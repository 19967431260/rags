网易云信即时通讯 SDK（NetEase IM SDK，以下简称 NIM SDK）提供聊天室管理功能，支持用户创建、进入、退出、查询、销毁聊天室，拥有完善的管理功能。

本文介绍聊天室管理相关 API。更多聊天室相关功能请参考 [聊天室概述](https://doc.yunxin.163.com/messaging2/client-apis/jYyNjkwMDA?platform=client)。

## API 概览

### 聊天室实例监听

API | 说明 | 起始版本
---- | ---- | ----
[addChatroomClientListener](#addChatroomClientListener) | 注册聊天室实例监听 | v10.5.0
[removeChatroomClientListener](#removeChatroomClientListener) | 取消注册聊天室实例监听 | v10.5.0

### 聊天室实例

API | 说明 | 起始版本
---- | ---- | ----
[newInstance](#newInstance) | 构造一个新的聊天室实例 | v10.5.0
[destroyInstance](#destroyInstance) | 销毁指定聊天室实例 | v10.5.0
[getInstance](#getInstance) | 获取指定聊天室实例 | v10.5.0
[getInstanceList](#getInstanceList) | 获取当前已经存在的聊天室实例列表 | v10.5.0
[destroyAll](#destroyAll) | 销毁当前的所有聊天室实例 | v10.5.0

### 聊天室操作

API | 说明 | 起始版本
---- | ---- | ----
[enter](#enter) | 进入聊天室 | v10.5.0
[exit](#exit) | 退出聊天室 | v10.5.0
[getChatroomInfo](#getChatroomInfo) | 获取聊天室信息 | v10.5.0
[getInstanceId](#getInstanceId) | 获取聊天室实例 ID | v10.5.0
[getChatroomService](#getChatroomService) | 获取聊天室服务 | v10.5.0
[getStorageService](#getStorageService) | 获取聊天室存储服务 | v10.5.0
[getChatroomQueueService](#getChatroomQueueService) | 获取聊天室队列服务 | v10.6.1

## 接口类

`V2NIMChatroomClient` 类提供聊天室相关接口，包括注册/注销聊天室实例监听，创建、进入、退出、销毁聊天室等接口。

## <span id="addChatroomClientListener">addChatroomClientListener</span>

**接口描述**

注册聊天室实例监听器。

注册成功后，当事件发生时，SDK 会返回对应的回调。

::: note note
- 建议在初始化后调用该方法。
- 全局只需注册一次。
- 该方法为同步。
:::

**回调事件**

- `onChatroomStatus`：聊天室状态变更回调，返回聊天室状态和错误信息。聊天室内所有成员均会收到该回调。
- `onChatroomEntered`：进入聊天室回调。
- `onChatroomExited`：退出聊天室回调，返回退出原因。
- `onChatroomKicked`：被踢出聊天室回调，返回被踢详情。被踢出聊天室后，会同时触发 `onChatroomExited` 回调。

**示例代码**

```Dart
//首先添加监听
await chatroomClient.addChatroomClientListener();
//添加成功后可以监听回到
chatroomClient!.onChatroomEntered.listen((event) {
          //print('ChatroomClient:onChatroomEntered');
        }),
        chatroomClient!.onChatroomExited.listen((event) {
         // print('ChatroomClient:onChatroomExited');
        }),
        chatroomClient!.onChatroomStatus.listen((event) {
          //print('ChatroomClient:onChatroomStatus');
        }),
        chatroomClient!.onChatroomKicked.listen((event) {
          // print('ChatroomClient:onChatroomKicked');
        })
```

## <span id="removeChatroomClientListener">removeChatroomClientListener</span>

**接口描述**

取消注册聊天室实例监听器。

::: note note
该方法为同步。
:::

**示例代码**

```Dart
//取消后 SDK 将不会再触发回调
chatroomClient!.removeChatroomClientListener();
```

## <span id="newInstance">newInstance</span>

**接口描述**

构造一个新的聊天室实例。

::: note note
- 聊天室功能需要额外开通。构建聊天室实例前，请确保已在 [网易云信控制台](https://app.yunxin.163.com/global/home) 上 [开通和配置聊天室](https://doc.yunxin.163.com/console/concept/zYyMjAzNDY?platform=console)。
- 建议保存实例 ID（`instanceId`）而不是构造的实例本身。
- 该方法为静态、同步。
:::

**参数说明**

```Dart
Future<NIMResult<V2NIMChatroomClient?>> newInstance()
```

**示例代码**

```Dart
chatroomClient = (await V2NIMChatroomClient.newInstance()).data;
```

**返回值**

[`V2NIMChatroomClient`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomClient) 聊天室实例

**相关回调**

无。

## <span id="destroyInstance">destroyInstance</span>

**接口描述**

销毁指定聊天室实例。

::: note note
该方法为静态、同步。
:::

**参数说明**

```Dart
Future<NIMResult<void>> destroyInstance(int instanceId)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `instanceId` | int | 是 | 聊天室实例 ID。如果为空、不合法、不存在则返回 191004 参数错误。 |

**示例代码**

```Dart
V2NIMChatroomClient.destroyInstance(instanceId);
```

**返回值**

NIMResult\<void>

**相关回调**

无。

## <span id="getInstance">getInstance</span>

**接口描述**

获取指定聊天室实例。

::: note note
该方法为静态、同步。
:::

**参数说明**

```Dart
Future<NIMResult<V2NIMChatroomClient?>> getInstance(int instanceId)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `instanceId` | int | 是 | 聊天室实例 ID。如果为空、不合法、不存在则返回 191004 参数错误。 |

**示例代码**

```Dart
V2NIMChatroomClient.getInstance(instanceId);
```

**返回值**

[`V2NIMChatroomClient`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomClient) 聊天室实例

**相关回调**

无。

## <span id="getInstanceList">getInstanceList</span>

**接口描述**

获取所有当前存在的聊天室实例。

::: note note
该方法为静态、同步。
:::

**参数说明**

```Dart
Future<NIMResult<List<V2NIMChatroomClient>?>> getInstanceList()
```

**示例代码**

```Dart
var clients = (await V2NIMChatroomClient.getInstanceList()).data;
```

**返回值**

NIMResult\<List\<[`NIMChatroomClient`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMChatroomClient)>> 聊天室实例列表

**相关回调**

无。

## <span id="destroyAll">destroyAll</span>

**接口描述**

销毁所有当前存在的聊天室实例。

::: note note
该方法为静态、同步。
:::

**参数说明**

```Dart
Future<NIMResult<void>> destroyAll()
```

**示例代码**

```Dart
V2NIMChatroomClient.destroyAll();
```

**返回值**

NIMResult\<void>

**相关回调**

无。

## <span id="enter">enter</span>

**接口描述**

进入聊天室。

本地端或多端同步进入聊天室成功后：

- 如果已在 [网易云信控制台](https://app.yunxin.163.com/global/home) 开启了 **聊天室用户进出消息系统下发** 功能，聊天室内所有其他成员会收到回调 `onChatroomMemberEnter`。如未开启，可参考 [开通和配置聊天室功能](https://doc.yunxin.163.com/console/concept/zYyMjAzNDY?platform=console#配置聊天室子功能)。
- 聊天室实例（`instanceId`）与聊天室（`roomId`）形成绑定关系。

::: note note
调用该方法前，请确保已完成以下步骤：
- 在 [网易云信控制台](https://app.yunxin.163.com/global/home) 上 [开通和配置聊天室功能](https://doc.yunxin.163.com/console/concept/zYyMjAzNDY?platform=console)。
- 调用服务端 API [创建聊天室](https://doc.yunxin.163.com/messaging2/server-apis/DU5NjcwNTk?platform=server)。
:::

**参数说明**

```Dart
Future<NIMResult<V2NIMChatroomEnterResult>> enter(String roomId,V2NIMChatroomEnterParams enterParams)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `roomId` | String | 是 | 聊天室 ID。如果为空、不合法、不存在则返回 191004 参数错误。 |
| `enterParams` | [`V2NIMChatroomEnterParams`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomEnterParams) | 是 | 进入聊天室相关参数。 |

**示例代码**

```Dart
final links = await NimCore.instance.loginService
          .getChatroomLinkAddress(chatroomId1);
      

      final enterParams = V2NIMChatroomEnterParams(
          authType: NIMLoginAuthType.authTypeDefault,
          accountId: account,
          token: token);

      chatroomClient!.linkProvider =
          (int instanceId, String roomId, String accountId) async {
        return links.data!;
      };

      final enterResult = await chatroomClient!.enter(chatroomId1, enterParams);
```

**返回值**

NIMResult\<[`V2NIMChatroomEnterResult`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMChatroomEnterResult)> 进入聊天室结果

**相关回调**

`onChatroomMemberEnter`：当用户进入聊天室后，聊天室内所有其他用户会收到该回调

## <span id="exit">exit</span>

**接口描述**

退出聊天室。

本地端或多端同步退出聊天室成功后：

- 如果已在 [网易云信控制台](https://app.yunxin.163.com/global/home) 开启了 **聊天室用户进出消息系统下发** 功能，聊天室内所有其他成员会收到回调 `onChatroomMemberExit`。如未开启，可参考 [开通和配置聊天室功能](https://doc.yunxin.163.com/console/concept/zYyMjAzNDY?platform=console#聊天室子功能)。
- 聊天室实例（`instanceId`）与聊天室（`roomId`）解除绑定关系。

**参数说明**

```Dart
Future<NIMResult<void>> exit()
```

**示例代码**

```Dart
chatroomClient!.exit();
```

**返回值**

NIMResult\<void>

**相关回调**

`onChatroomMemberExit`：当用户退出聊天室后，聊天室内所有其他用户会收到该回调

## <span id="getChatroomInfo">getChatroomInfo</span>

**接口描述**

获取聊天室信息。

::: note note
调用该方法前，请确保已调用 [`enter`](#enter) 方法进入聊天室，否则返回 null。
:::

**参数说明**

```Dart
Future<NIMResult<V2NIMChatroomInfo>> getChatroomInfo()
```

**示例代码**

```Dart
final chatroomInfoResult = await chatroomClient?.getChatroomInfo();
```

**返回值**

[`V2NIMChatroomInfo`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomInfo) 聊天室信息

**相关回调**

无。

## <span id="getInstanceId">getInstanceId</span>

**接口描述**

获取聊天室实例 ID。

**参数说明**

```Dart
int? getInstanceId()
```

**示例代码**

```Dart
final instanceId = chatroomClient!.getInstanceId();
```

**返回值**

int 聊天室实例 ID

**相关回调**

无。

## <span id="getChatroomService">getChatroomService</span>

**接口描述**

获取聊天室服务。后续聊天室相关操作（聊天室成员、消息等）均在返回的 [`V2NIMChatroomService`](https://doc.yunxin.163.com/messaging2/client-apis/TQzMTI2MjU?platform=client) 类中实现。

::: note notice
调用该方法前，请确保已调用 [`enter`](#enter) 方法进入聊天室，否则返回 null。
:::

**参数说明**

```Dart
V2NIMChatroomService getChatroomService()
```

**示例代码**

```Dart
var chatroomService = chatroomClient?.getChatroomService();
```

**返回值**

[`V2NIMChatroomService`](https://doc.yunxin.163.com/messaging2/client-apis/TQzMTI2MjU?platform=client) 聊天室服务

**相关回调**

无。

## <span id="getChatroomQueueService">getChatroomQueueService</span>

**接口描述**

获取聊天室队列服务。后续聊天室队列相关操作均在返回的 [`V2NIMChatroomQueueService`](https://doc.yunxin.163.com/messaging2/client-apis/jU2NTkxNDE?platform=client) 类中实现。

**参数说明**

```Dart
V2NIMChatroomQueueService getChatroomQueueService()
```

**示例代码**

```Dart
var chatroomQueueService = chatroomClient?.getChatroomQueueService();
```

**返回值**

[`V2NIMChatroomQueueService`](https://doc.yunxin.163.com/messaging2/client-apis/jU2NTkxNDE?platform=client) 聊天室队列服务

**相关回调**

无。

## <span id="getStorageService">getStorageService</span>

**接口描述**

获取 IM 存储服务。后续收发富媒体/自定义消息所需存储相关接口均在返回的 [`StorageService`](https://doc.yunxin.163.com/messaging2/client-apis/TUwMzk0MDg?platform=client) 类中实现。

**参数说明**

```Dart
StorageService getStorageService()
```

**示例代码**

```Dart
final storageService  = chatroomClient!.getStorageService();
```

**返回值**

[`StorageService`](https://doc.yunxin.163.com/messaging2/client-apis/TUwMzk0MDg?platform=client) 聊天室存储服务

**相关回调**

无。