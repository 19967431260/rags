网易云信即时通讯 SDK（NetEase IM SDK，以下简称 NIM SDK）提供聊天室服务，支持用户收发聊天室消息、管理聊天室成员、维护聊天室信息。

本文介绍聊天室服务相关 API。更多聊天室相关功能请参考 [聊天室概述](https://doc.yunxin.163.com/messaging2/client-apis/jYyNjkwMDA?platform=client)。

## API 概览

### 聊天室监听

API | 说明 | 起始版本
---- | ---- | ----
[addChatroomListener](#addChatroomListener) | 注册聊天室监听 | v10.5.0
[removeChatroomListener](#removeChatroomListener) | 取消注册聊天室监听 | v10.5.0

### 聊天室消息

API | 说明 | 起始版本
---- | ---- | ----
[sendMessage](#sendMessage) | 发送聊天室消息 | v10.5.0
[cancelMessageAttachmentUpload](#cancelMessageAttachmentUpload) | 取消文件类附件上传 | v10.5.0
[getMessageList](#getMessageList) | 分页获取聊天室历史消息 | v10.5.0
[getMessageListByTag](#getMessageListByTag) | 根据标签分页获取聊天室消息列表 | v10.5.0

### 聊天室成员

API | 说明 | 起始版本
---- | ---- | ----
[kickMember](#kickMember) | 将成员踢出聊天室 | v10.5.0
[getMemberListByOption](#getMemberListByOption) | 分页获取聊天室成员列表 | v10.5.0
[getMemberByIds](#getMemberByIds) | 批量获取聊天室成员列表 | v10.5.0
[getMemberListByTag](#getMemberListByTag) | 根据标签分页获取聊天室成员列表 | v10.5.0
[getMemberCountByTag](#getMemberCountByTag) | 根据标签获取聊天室成员数量 | v10.5.0
[updateMemberRole](#updateMemberRole) | 更新聊天室成员角色 | v10.5.0
[setMemberBlockedStatus](#setMemberBlockedStatus) | 设置聊天室成员黑名单状态 | v10.5.0
[setMemberChatBannedStatus](#setMemberChatBannedStatus) | 设置聊天室成员禁言状态 | v10.5.0
[setMemberTempChatBanned](#setMemberTempChatBanned) | 设置聊天室成员临时禁言状态 | v10.5.0
[setTempChatBannedByTag](#setTempChatBannedByTag) | 设置指定标签下的聊天室成员临时禁言状态 | v10.5.0
[updateSelfMemberInfo](#updateSelfMemberInfo) | 更新本人的聊天室成员信息 | v10.5.0

### 聊天室信息

API | 说明 | 起始版本
---- | ---- | ----
[updateChatroomInfo](#updateChatroomInfo) | 更新聊天室信息 | v10.5.0
[updateChatroomLocationInfo](#updateChatroomLocationInfo) | 更新聊天室空间坐标信息 | v10.5.0
[updateChatroomTags](#updateChatroomTags) | 更新聊天室标签信息 | v10.5.0

## 接口类

`V2NIMChatroomService` 类提供聊天室服务接口，包括注册/注销聊天室监听器、收发聊天室消息、管理聊天室成员、维护聊天室信息等。

## 使用前提

在使用 `V2NIMChatroomService` 类中的 API 前，需要先调用 [`V2NIMChatroomClient#getChatroomService`](https://doc.yunxin.163.com/messaging2/client-apis/zE5NTI4MjU?platform=client#getChatroomService) 方法获取 `V2NIMChatroomService` 类。

## <span id="addChatroomListener">addChatroomListener</span>

**接口描述**

注册聊天室监听器。

注册成功后，当事件发生时，SDK 会返回对应的回调。

::: note note
- 建议在初始化后调用该方法。
- 全局只需注册一次。
- 该方法为同步。
:::

**回调事件**

- `onSendMessage`：本端发送聊天室消息状态回调。本端发送聊天室消息或插入聊天室消息成功后，SDK 会返回该回调。
- `onReceiveMessages`：聊天室消息接收回调。本端接收到消息，或其他端同步的消息时会触发该回调。
- `onSendMessageProgress`：聊天室发送消息进度回调。本端发送消息后，SDK 会返回消息的发送进度回调。
- `onChatroomInfoUpdated`：聊天室资料信息更新回调。聊天室内所有成员均会收到该回调。
- `onChatroomMemberEnter`：成员进入聊天室回调。聊天室内所有成员均会收到该回调。
- `onChatroomMemberExit`：成员退出聊天室回调。当成员退出或被踢出聊天室，聊天室内所有成员均会收到该回调。
- `onChatroomMemberRoleUpdated`：聊天室成员角色类型变更回调。聊天室内所有成员均会收到该回调。
- `onChatroomMemberInfoUpdated`：聊天室成员信息变更回调。聊天室内所有成员均会收到该回调。
- `onSelfChatBannedUpdated`：聊天室成员本人临时禁言状态变更回调。
- `onSelfTempChatBannedUpdated`：聊天室成员本人临时禁言状态变更回调。
- `onChatroomChatBannedUpdated`：聊天室整体禁言状态变更回调。
- `onMessageRevokedNotification`：聊天室消息撤回回调。
- `onChatroomTagsUpdated`：聊天室标签信息变更回调。

**示例代码**

```Dart
//首先添加监听
await chatroomClient?.getChatroomService().addChatroomListener();
//然后设置监听
chatroomClient!.getChatroomService().onReceiveMessages.listen((event) {
        print('getChatroomService:onReceiveMessages');
      });
chatroomClient!.getChatroomService().onSendMessage.listen((event) {
  print('getChatroomService:onSendMessage ${event.message?.toJson()}');
});
chatroomClient!.getChatroomService().onChatroomMemberEnter.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onChatroomChatBannedUpdated.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onChatroomInfoUpdated.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onChatroomMemberExit.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onChatroomMemberInfoUpdated.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onChatroomMemberRoleUpdated.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onChatroomTagsUpdated.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onMessageRevokedNotification.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onSelfChatBannedUpdated.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onSelfTempChatBannedUpdated.listen((event) {
//todo something
});
chatroomClient!.getChatroomService().onSendMessageProgress.listen((event) {
//todo something
});
```

## <span id="removeChatroomListener">removeChatroomListener</span>

**接口描述**

取消注册聊天室监听器。

::: note note
该方法为同步。
:::

**示例代码**

```Dart
chatroomClient!.getChatroomService().removeChatroomListener();
```

## <span id="sendMessage">sendMessage</span>

**接口描述**

发送一条聊天室消息。

本端发送消息成功后，聊天室内所有成员会收到消息接收回调 `onReceiveMessages`。

::: note note
- 发送消息前，请先调用 [`V2NIMChatroomMessageCreator`](https://doc.yunxin.163.com/messaging2/client-apis/TAxMjQ2Mzk?platform=client) 中的接口创建不同类型的聊天室消息。
- 如因网络等问题导致消息发送失败，可再次调用该方法重发消息。
:::

**参数说明**

```Dart
Future<NIMResult<V2NIMSendChatroomMessageResult>> sendMessage(V2NIMChatroomMessage message,V2NIMSendChatroomMessageParams params)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `message` | [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomMessage) | 是 | 聊天室消息对象，通过调用 [`V2NIMChatroomMessageCreator`](https://doc.yunxin.163.com/messaging2/client-apis/TY4MDg4MTk?platform=client) 中的接口创建。如果为空或不存在则返回 191004 参数错误。 |
| `params` | [`V2NIMSendChatroomMessageParams`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMSendChatroomMessageParams) | 否 | 聊天室消息发送配置参数，包括发送、抄送、反垃圾、标签、空间位置等配置。 |

**示例代码**

```Dart
V2NIMSendChatroomMessageParams params = V2NIMSendChatroomMessageParams();
    final messageSender = await chatroomService?.sendMessage(message, params);
```

**返回值**

NIMResult\<[`V2NIMSendChatroomMessageResult`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMSendChatroomMessageResult)> 聊天室消息发送结果

**相关回调**

- `onSendMessage`：本端发送聊天室消息状态回调。本端发送聊天室消息或插入聊天室消息成功后，SDK 会返回该回调。
- `onReceiveMessages`：聊天室消息接收回调。本端接收到消息，或其他端同步的消息时会触发该回调。
- `onSendMessageProgress`：聊天室发送消息进度回调。本端发送消息后，SDK 会返回消息的发送进度回调。

## <span id="cancelMessageAttachmentUpload">cancelMessageAttachmentUpload</span>

**接口描述**

取消上传聊天室消息附件。

::: note note
- 该方法仅支持取消文件类消息（图片、视频、文件）的附件上传。
- 无法取消已经上传成功的附件。
- 如果取消上传成功，消息附件上传状态会转变为上传失败 `NIMMessageAttachmentUploadState.failed`，对应的文件类消息会发送失败。
:::

**参数说明**

```Dart
Future<NIMResult<void>> cancelMessageAttachmentUpload(V2NIMChatroomMessage message)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `message` | [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomMessage) | 是 | 取消附件上传的聊天室消息对象。该消息必须为图片、视频、文件类型。 |

**示例代码**

```Dart
chatroomService?.cancelMessageAttachmentUpload(message);
```

**返回值**

NIMResult\<void>

**相关回调**

无。

## <span id="getMessageList">getMessageList</span>

**接口描述**

按查询配置项分页获取所有历史消息。

该方法用于分页获取聊天室消息数据，直到获取聊天室全量消息，获取到的消息包括聊天室通知消息。

**参数说明**

```Dart
Future<NIMResult<List<V2NIMChatroomMessage>>> getMessageList(V2NIMChatroomMessageListOption option)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `option` | [`V2NIMChatroomMessageListOption`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomMessageListOption) | 是 | 聊天室消息查询选项，支持查询指定时间段和消息类型的聊天室历史消息。 |

**示例代码**

```Dart
final option = V2NIMChatroomMessageListOption(limit: 100);
    final messageList = (await chatroomService.getMessageList(option));
```

**返回值**

NIMResult<List<[`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMChatroomMessage)>> 聊天室消息列表

**相关回调**

无。

## <span id="getMessageListByTag">getMessageListByTag</span>

**接口描述**

分页获取指定标签的所有聊天室历史消息。

该方法用于分页获取聊天室消息数据，直到获取聊天室全量消息，获取到的消息不包括已删除消息。

::: note note
调用前请确保指定标签存在。
:::

**参数说明**

```Dart
Future<NIMResult<List<V2NIMChatroomMessage>>> getMessageListByTag(V2NIMChatroomTagMessageOption messageOption)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `messageOption` | [`V2NIMChatroomTagMessageOption`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomTagMessageOption) | 是 | 按标签查询聊天室消息选项。 |

**示例代码**

```Dart
final option = V2NIMChatroomTagMessageOption();
    final messageList = (await chatroomService.getMessageListByTag(option));
```

**返回值**

NIMResult<List<[`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMChatroomMessage)>> 聊天室消息列表

**相关回调**

无。

## <span id="kickMember">kickMember</span>

**接口描述**

将聊天室成员踢出聊天室。

本地端或多端同步踢出聊天室成员成功后：

- 被踢方收到被踢出聊天室回调 `onChatroomKicked`。
- 聊天室内的其他成员会收到成员退出聊天室回调 `onChatroomMemberExit`。
- 聊天室内所有成员收到一条通知类消息，类型为 [`V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_KICKED (7)`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomMessage#V2NIMChatroomMessageNotificationType)。

::: note notice
- 仅允许聊天室创建者和管理员调用该接口，否则返回错误码 109432。
- 如果被踢方为管理员，则仅允许聊天室创建者操作，否则返回错误码 109427。
:::

**参数说明**

```Dart
Future<NIMResult<void>> kickMember(String accountId,{String? notificationExtension})
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `accountId` | String | 是 | 踢出聊天室的成员账号，如果为空、不合法、不存在，则返回 191004 参数错误。 |
| `notificationExtension` | String | 否 | 本次操作生成的通知中的扩展字段。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    chatroomService?.kickMember('accountId');
```

**返回值**

NIMResult\<void>

**相关回调**

- `onChatroomKicked`：被踢出聊天室的成员会收到该回调
- `onChatroomMemberExit`：聊天室其他成员会收到成员退出聊天室回调

## <span id="getMemberListByOption">getMemberListByOption</span>

**接口描述**

分页获取所有聊天室成员列表。

**参数说明**

```Dart
Future<NIMResult<V2NIMChatroomMemberListResult>> getMemberListByOption(V2NIMChatroomMemberQueryOption queryOption)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `queryOption` | [`V2NIMChatroomMemberQueryOption`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomMemberQueryOption) | 是 | 聊天室成员分页查询选项。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    final queryOption = V2NIMChatroomMemberQueryOption();
    chatroomService?.getMemberListByOption(queryOption);
```

**返回值**

[`V2NIMChatroomMemberListResult`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomMemberListResult) 聊天室成员查询结果

**相关回调**

无。

## <span id="getMemberByIds">getMemberByIds</span>

**接口描述**

批量获取指定聊天室成员列表。

::: note note
- 单次查询数量上限为 200。
- 返回聊天室成员信息按照输入 `accountIds` 顺序排序。
:::

**参数说明**

```Dart
Future<NIMResult<List<V2NIMChatroomMember>>> getMemberByIds(List<String> accountIds)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `accountIds` | List\<String> | 是 | 如果输入为空、数量为 0 或者超过 200，都会返回 191004 参数错误。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    chatroomService?.getMemberByIds(['memberId']);
```

**返回值**

NIMResult\<List\<[`V2NIMChatroomMember`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomMember)>> 聊天室信息列表

**相关回调**

无。

## <span id="getMemberListByTag">getMemberListByTag</span>

**接口描述**

分页获取指定标签下的所有聊天室成员。

::: note note
调用前请确保指定标签存在。
:::

**参数说明**

```Dart
Future<NIMResult<V2NIMChatroomMemberListResult>> getMemberListByTag(V2NIMChatroomTagMemberOption option)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `option` | [`V2NIMChatroomTagMemberOption`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomTagMemberOption) | 是 | 按标签查询聊天室成员选项。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    var option = V2NIMChatroomTagMemberOption();
    var result = await chatroomService?.getMemberListByTag(option);
```

**返回值**

NIMResult<[`V2NIMChatroomMemberListResult`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomMemberListResult)> 聊天室成员查询结果

**相关回调**

无。

## <span id="getMemberCountByTag">getMemberCountByTag</span>

**接口描述**

获取指定标签下的聊天室成员人数。

::: note note
调用前请确保指定标签存在。
:::

**参数说明**

```Dart
Future<NIMResult<int>> getMemberCountByTag(String tag)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `tag` | String | 是 | 指定的聊天室标签。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    var result = await chatroomService?.getMemberCountByTag('tag');
```

**返回值**

NIMResult\<int> 聊天室成员人数

**相关回调**

无。

## <span id="updateMemberRole">updateMemberRole</span>

**接口描述**

更新聊天室成员角色类型。

本地端或多端同步更新聊天室成员角色类型成功后，聊天室内所有成员收到成员角色类型变更回调 `onChatroomMemberRoleUpdated` 及通知类消息，消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_ROLE_UPDATE(18)`。

::: note notice
- 仅允许聊天室创建者和管理员调用该接口，否则返回错误码 109432。
- 如果变更为管理员、或将管理员变更为其他角色，则仅允许聊天室创建者操作，否则返回错误码 109427。
- 不允许操作虚构用户和匿名游客。
:::

**参数说明**

```Dart
Future<NIMResult<void>> updateMemberRole(String accountId,V2NIMChatroomMemberRoleUpdateParams updateParams)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `accountId` | String | 是 | 聊天室用户账号，如果为空、不合法、不存在，则返回 191004 参数错误。 |
| `updateParams` | [`V2NIMChatroomMemberRoleUpdateParams`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomMemberRoleUpdateParams) | 是 | 聊天室成员更新参数。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
final params = V2NIMChatroomMemberRoleUpdateParams();
var result = await chatroomService?.updateMemberRole('account_id', params);
```

**返回值**

NIMResult\<void>

**相关回调**

`onChatroomMemberRoleUpdated` 聊天室成员角色变更回调

## <span id="setMemberBlockedStatus">setMemberBlockedStatus</span>

**接口描述**

设置聊天室成员黑名单状态。[聊天室黑名单用户](https://doc.yunxin.163.com/messaging2/client-apis/Dc4NTY1ODQ?platform=client#技术原理) 与聊天室服务器断开连接，无法收发聊天室消息。

本地端或多端同步设置聊天室成员黑名单状态成功后：

- 聊天室内所有成员收到成员信息变更回调 `onChatroomMemberInfoUpdated`。
- 将成员加入黑名单后（设置 `blocked` 参数为 `true`），被拉黑方收到退出聊天室回调 `onChatroomExited`。所有聊天室成员会收到成员退出聊天室回调 `onChatroomMemberExit`，同时会收到对应的聊天室通知消息，通知消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_BLOCK_ADDED(2)`。
- 将成员从黑名单移除后（设置 `blocked` 参数为 `false`），所有聊天室成员收到成员加入聊天室回调 `onChatroomMemberEnter`，同时会收到对应的聊天室通知消息，类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_BLOCK_REMOVED(3)`。

::: note notice
- 仅允许聊天室创建者和管理员调用该接口，否则返回错误码 109432。
- 如果被操作方为管理员，则仅允许聊天室创建者操作，否则返回错误码 109427。
- 不允许操作虚构用户和匿名游客。
:::

**参数说明**

```Dart
Future<NIMResult<void>> setMemberBlockedStatus(String accountId,bool blocked,{String? notificationExtension})
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `accountId` | String | 是 | 聊天室用户账号，如果为空、不合法、不存在，则返回 191004 参数错误。<br>不允许传入虚构用户和匿名游客的用户账号。 |
| `blocked` | bool | 是 | 是否加入黑名单：<li>true：加入黑名单 <li>false：从黑名单移除 |
| `notificationExtension` | String | 否 | 本次操作生成的通知中的扩展字段。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    var result = await chatroomService?.setMemberBlockedStatus('account_id',true);
```

**返回值**

NIMResult\<void>

**相关回调**

- `onChatroomMemberInfoUpdated` 聊天室成员信息更新回调
- `onChatroomMemberEixt` 成员退出聊天室回调

## <span id="setMemberChatBannedStatus">setMemberChatBannedStatus</span>

**接口描述**

设置聊天室成员禁言状态。[聊天室禁言用户](https://doc.yunxin.163.com/messaging2/client-apis/Dc4NTY1ODQ?platform=client#技术原理) 用户无法在聊天室发消息。

本地端或多端同步设置聊天室成员禁言状态成功后：

- 被禁言成员：收到禁言状态变更回调 `onSelfChatBannedUpdated`。
- 聊天室内所有成员：
    - 收到成员信息变更回调 `onChatroomMemberInfoUpdated`。
    - 将成员禁言后（设置 `chatBanned` 参数为 `true`），所有聊天室成员会收到聊天室通知消息，通知消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_CHAT_BANNED_ADDED(4)`。将成员解除禁言后（设置 `chatBanned` 参数为 `false`），所有聊天室成员收到聊天室通知消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_CHAT_BANNED_REMOVED(5)`。

::: note notice
- 仅允许聊天室创建者和管理员调用该接口，否则返回错误码 109432。
- 如果被操作方为管理员，则仅允许聊天室创建者操作，否则返回错误码 109427。
- 不允许操作虚构用户和匿名游客。
:::

**参数说明**

```Dart
Future<NIMResult<void>> setMemberChatBannedStatus(String accountId,bool chatBanned,{String? notificationExtension})
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `accountId` | String | 是 | 聊天室用户账号，如果为空、不合法、不存在，则返回 191004 参数错误。<br>不允许传入虚构用户和匿名游客的用户账号。 |
| `chatBanned` | bool | 是 | 是否为禁言状态 |
| `notificationExtension` | String | 否 | 本次操作生成的通知中的扩展字段。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    var result = await chatroomService?.setMemberChatBannedStatus('account_id',true);
```

**返回值**

NIMResult\<void>

**相关回调**

- `onSelfChatBannedUpdated` 聊天室成员本人禁言状态变更回调
- `onChatroomMemberInfoUpdated` 聊天室成员信息更新回调

## <span id="setMemberTempChatBanned">setMemberTempChatBanned</span>

**接口描述**

设置聊天室成员临时禁言状态。

被临时禁言后，无法在聊天室发送消息。临时禁言结束后，恢复发送消息权限。

本地端或多端同步设置聊天室成员临时禁言状态成功后：

- 被禁言成员：收到临时禁言状态变更回调 `onSelfTempChatBannedUpdated`。
- 聊天室内所有成员：
    - 收到成员信息变更回调 `onChatroomMemberInfoUpdated`。
    - 将成员临时禁言后（`tempChatBannedDuration` 不为 0），所有聊天室成员会收到聊天室通知消息，通知消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_TEMP_CHAT_BANNED_ADDED(8)`。将成员解除临时禁言后（`tempChatBannedDuration` 为 0），所有聊天室成员收到聊天室通知消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_TEMP_CHAT_BANNED_REMOVED(9)`。

::: note notice
- 仅允许聊天室创建者和管理员调用该接口，否则返回错误码 109432。
- 如果被操作方为管理员，则仅允许聊天室创建者操作，否则返回错误码 109427。
- 不允许操作虚构用户和匿名游客。
:::

**参数说明**

```Dart
Future<NIMResult<void>> setMemberTempChatBanned(String accountId,int tempChatBannedDuration,bool notificationEnabled,{String? notificationExtension})
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `accountId` | String | 是 | 聊天室用户账号，如果为空、不合法、不存在，则返回 191004 参数错误。<br>不允许传入虚构用户和匿名游客的用户账号。 |
| `tempChatBannedDuration` | int | 是 | 临时禁言时长（秒）。不可超过 30 天。<br>设置为 0 表示 **解除临时禁言**。 |
| `notificationEnabled` | boo | 是 | 操作后是否需要通知。 |
| `notificationExtension` | String | 否 | 本次操作生成的通知中的扩展字段。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    var result = await chatroomService?.setMemberTempChatBanned('account_id',60,true);
```

**返回值**

NIMResult\<void>

**相关回调**

- `onSelfTempChatBannedUpdated` 聊天室成员本人临时禁言状态变更回调
- `onChatroomMemberInfoUpdated` 聊天室成员信息更新回调

## <span id="setTempChatBannedByTag">setTempChatBannedByTag</span>

**接口描述**

设置聊天室标签临时禁言。设置后，被临时禁言的聊天室标签中的所有成员，无法在聊天室发送消息。临时禁言结束后，恢复发送消息权限。

本地端或多端同步设置聊天室标签临时禁言成功后：

::: note notice
只有在配置参数 `V2NIMChatroomTagTempChatBannedParams` 中传入 `notifyTargetTags`（需要通知的聊天室标签），临时禁言成功后，设置的聊天室标签下的成员才会收到以下回调和通知，若不传入 `notifyTargetTags`，则不会触发。
:::

- 被临时禁言的聊天室标签成员：收到本人临时禁言状态变更回调 `onSelfTempChatBannedUpdated`。
- 聊天室内所有成员：
    - 收到被临时禁言的成员信息变更回调 `onChatroomMemberInfoUpdated`。
    - 收到聊天室通知消息，通知消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_TEMP_CHAT_BANNED_ADDED(8)`。解除聊天室标签临时禁言后，所有聊天室成员收到聊天室通知消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_TEMP_CHAT_BANNED_REMOVED(9)`。

::: note notice
- 仅允许聊天室创建者和管理员调用该接口，否则返回错误码 109432。
- 如果被操作方为管理员，则仅允许聊天室创建者操作，否则返回错误码 109427。
- 不允许操作虚构用户和匿名游客。
- 调用前请确保指定标签存在。
:::

**参数说明**

```Dart
Future<NIMResult<void>> setTempChatBannedByTag(V2NIMChatroomTagTempChatBannedParams params)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `params` | [`V2NIMChatroomTagTempChatBannedParams`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomTagTempChatBannedParams) | 是 | 聊天室成员临时禁言标签参数。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    final params = V2NIMChatroomTagTempChatBannedParams();
    var result = await chatroomService?.setTempChatBannedByTag(params);
```

**返回值**

NIMResult\<void>

**相关回调**

- `onSelfTempChatBannedUpdated` 聊天室成员本人临时禁言状态变更回调
- `onChatroomMemberInfoUpdated` 聊天室成员信息更新回调

## <span id="updateSelfMemberInfo">updateSelfMemberInfo</span>

**接口描述**

更新本人的聊天室成员信息。

本地端或多端同步更新成功后，聊天室内所有成员会收到：

- 成员信息变更回调 `onChatroomMemberInfoUpdated`。
- 若 `notificationEnabled` 设置为 true，还会收到类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_MEMBER_INFO_UPDATED(10)` 的聊天室通知消息。

**参数说明**

```Dart
Future<NIMResult<void>> updateSelfMemberInfo(V2NIMChatroomSelfMemberUpdateParams updateParams,NIMAntispamConfig? antispamConfig)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `updateParams` | [`V2NIMChatroomSelfMemberUpdateParams`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomSelfMemberUpdateParams) | 是 | 本人聊天室成员信息更新参数。 |
| `antispamConfig` | [`NIMAntispamConfig`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMAntispamConfig) | 否 | 易盾反垃圾相关配置。<note type=notice>使用网易云信安全通无需配置该参数。</note> |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    final params = V2NIMChatroomSelfMemberUpdateParams();
    var result = await chatroomService?.updateSelfMemberInfo(params,null);
```

**返回值**

NIMResult\<void>

**相关回调**

`onChatroomMemberInfoUpdated` 聊天室成员信息更新回调

## <span id="updateChatroomInfo">updateChatroomInfo</span>

**接口描述**

更新聊天室信息。

本地端或多端同步更新成功后，聊天室内所有成员会收到聊天室信息变更回调 `onChatroomInfoUpdated`，以及聊天室通知消息，通知消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_ROOM_INFO_UPDATED(6)`。

**参数说明**

```Dart
Future<NIMResult<void>> updateChatroomInfo(V2NIMChatroomUpdateParams updateParams,NIMAntispamConfig? antispamConfig)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `updateParams` | [`V2NIMChatroomUpdateParams`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomUpdateParams) | 是 | 聊天室信息更新参数。 |
| `antispamConfig` | [`NIMAntispamConfig`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMAntispamConfig) | 否 | 易盾反垃圾相关配置。<note type=notice>使用网易云信安全通无需配置该参数。</note> |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    final params = V2NIMChatroomUpdateParams();
    var result = await chatroomService?.updateChatroomInfo(params,null);
```

**返回值**

NIMResult\<void>

**相关回调**

`onChatroomInfoUpdated` 聊天室信息更新回调

## <span id="updateChatroomLocationInfo">updateChatroomLocationInfo</span>

**接口描述**

更新聊天室空间坐标信息。

**参数说明**

```Dart
Future<NIMResult<void>> updateChatroomLocationInfo(V2NIMChatroomLocationConfig locationConfig)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `locationConfig` | [`V2NIMChatroomLocationConfig`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomLocationConfig) | 是 | 聊天室坐标信息配置参数。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    final params = V2NIMChatroomLocationConfig();
    var result = await chatroomService?.updateChatroomLocationInfo(params);
```

**返回值**

NIMResult\<void>

**相关回调**

无。

## <span id="updateChatroomTags">updateChatroomTags</span>

**接口描述**

批量更新聊天室标签信息。

本地端或多端同步更新成功后，聊天室内所有成员会收到聊天室标签信息变更回调 `onChatroomTagsUpdated`，以及聊天室通知消息，通知消息类型为 `V2NIMChatroomMessageNotificationType.V2NIM_CHATROOM_MESSAGE_NOTIFICATION_TYPE_TAGS_UPDATE(17)`。

::: note note
调用前请确保指定标签存在。
:::

**参数说明**

```Dart
Future<NIMResult<void>> updateChatroomTags(V2NIMChatroomTagsUpdateParams updateParams)
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `updateParams` | [`V2NIMChatroomTagsUpdateParams`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatroomTagsUpdateParams) | 是 | 聊天室标签信息更新参数。 |

**示例代码**

```Dart
final chatroomService = chatroomClient?.getChatroomService();
    final params = V2NIMChatroomTagsUpdateParams();
    var result = await chatroomService?.updateChatroomTags(params);
```

**返回值**

NIMResult\<void>

**相关回调**

`onChatroomTagsUpdated` 聊天室标签更新回调

<!--未实现
## <span id="registerCustomAttachmentParser">registerCustomAttachmentParser</span>

**接口描述**

注册自定义消息附件解析器，解析消息类型为 100 的消息附件。

::: note note 
- 可以注册多个解析器，后注册的优先级高，优先派发， 一旦被处理，则不继续派发。
- 如果同一个类型多个解析均可以解析，则优先级高的先解析， 解析后直接返回。
- 按需注册，若不再需要，请及时反注册。
- 该方法为同步。
:::

**参数说明**

```Dart

```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `parser` | [`V2NIMChatRoomMessageCustomAttachmentParser`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatRoomMessageCustomAttachmentParser) | 是 | 注册的自定义消息附件解析器。 |

**示例代码**

```Dart

```

**返回值**

无。

## <span id="unregisterCustomAttachmentParser">unregisterCustomAttachmentParser</span>

**接口描述**

反注册自定义消息附件解析器。

::: note note 
该方法为同步。
:::

**参数说明**

```Dart

```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `parser` | [`V2NIMChatRoomMessageCustomAttachmentParser`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#V2NIMChatRoomMessageCustomAttachmentParser) | 是 | 反注册的自定义消息附件解析器 |


**示例代码**

```Dart

```

**返回值**

无。
-->