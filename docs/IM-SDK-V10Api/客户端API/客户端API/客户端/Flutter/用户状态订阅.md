网易云信即时通讯 SDK（NetEase IM SDK，以下简称 NIM SDK）提供订阅用户状态功能，支持订阅/取消订阅用户状态，发布自定义的用户状态，查询用户状态订阅关系等。用户的状态包括在线状态和自定义状态。

本文介绍用户状态订阅相关的 API。

## API 概览

### 用户状态订阅监听

API | 说明 | 起始版本
---- | ---- | ----
[listen](#listen) | 注册用户状态订阅相关监听器 | v10.3.4 |
[cancel](#cancel) | 取消注册用户状态订阅相关监听器 | v10.3.4 |

### 用户状态订阅操作

API | 说明 | 起始版本
---- | ---- | ----
[subscribeUserStatus](#subscribeUserStatus) | 订阅用户状态 | v10.3.4
[unsubscribeUserStatus](#unsubscribeUserStatus ) | 取消订阅用户状态 | v10.3.4
[publishCustomUserStatus](#publishCustomUserStatus) | 发布用户自定义状态 | v10.3.4
[queryUserStatusSubscriptions](#queryUserStatusSubscriptions) | 查询用户状态订阅关系 | v10.3.4

## 接口类

`SubscriptionService` 类提供用户状态订阅相关接口，包括注册用户状态订阅监听，获取、订阅用户状态，发布自定义状态等接口。

## <span id="listen">listen</span>

**接口描述**

注册用户状态订阅监听器。

注册成功后，当事件发生时，SDK 会返回对应的回调。

::: note note
- 建议在初始化后调用该方法。

- 全局只需注册一次。

- 该方法为同步。
:::

**回调事件**

`onUserStatusChanged`：其他用户的用户状态变更回调，包括在线状态和自定义状态。同账号发布状态时，多端也会同步收到回调。

**示例代码**

```Dart
subsriptions.add(
      NimCore.instance.subscriptionService.onUserStatusChanged.listen((events){
         //do something
      })
    );
```

## <span id="cancel">cancel</span>

**接口描述**

取消注册用户状态订阅相关监听。

::: note note
该方法为同步。
:::

**示例代码**

```Dart
subsriptions.forEach((subsription) {
      subsription.cancel();
    });
```

## <span id="subscribeUserStatus">subscribeUserStatus</span>

**接口描述**

订阅用户状态，包括在线状态或用户自定义的状态。

成功订阅用户状态后，当订阅的用户状态有变更时，会触发 `onUserStatusChanged` 回调。

::: note note
- 用户状态事件订阅时单向的，双方需要各自自行订阅。
- 单次最多订阅 100 个用户的状态，可多次调用该接口，订阅更多的用户状态，但是总订阅人数最多为 3000，若超限，则默认删除最老的订阅，即有效订阅最多为 3000 个。
- 若同一账号多端重复订阅，订阅有效期默认会覆盖（新的覆盖前一次时长）。但如果在订阅后 30s 内重复订阅，该操作会被直接丢弃。因此建议 30s 后再进行操作。在 30s 后重复订阅，新设置的有效期才会覆盖之前的有效期。
- 若传入的账号 ID 都不存在，则调用接口失败。若部分账号 ID 存在，则调用接口成功。调用结果只返回账号 ID 存在的用户状态，错误的账号 ID 不返回。
:::

**参数说明**

```Dart
Future<NIMResult<List<String>>> subscribeUserStatus(
    NIMSubscribeUserStatusOption option) async {
  return _platform.subscribeUserStatus(option);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `option` | [`NIMSubscribeUserStatusOption`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMSubscribeUserStatusOption) | 是 | 订阅用户状态的配置参数。 |

**示例代码**

```Dart
var option = NIMSubscribeUserStatusOption(accountIds: ['account'])

    NimCore.instance.subscriptionService.subscribeUserStatus(option).then((value) async {
       if(value.isSuccess){
         // 订阅成功
       }
    })
```

**返回值**

NIMResult\<List\<String>> 订阅的用户账号列表

**相关回调**

`onUserStatusChanged`

## <span id="unsubscribeUserStatus">unsubscribeUserStatus</span>

**接口描述**

取消用户状态的订阅。

**参数说明**

```Dart
Future<NIMResult<List<String>>> unsubscribeUserStatus(
    NIMUnsubscribeUserStatusOption option) async {
  return _platform.unsubscribeUserStatus(option);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `option` | [`NIMUnsubscribeUserStatusOption`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMUnsubscribeUserStatusOption) | 是 | 取消订阅用户状态的配置参数。 |

**示例代码**

```Dart
var option = NIMUnsubscribeUserStatusOption(accountIds: ['account'])

    NimCore.instance.subscriptionService.unsubscribeUserStatus(option).then((value) async {
       if(value.isSuccess){
         // 取消订阅成功
       }
    });
```

**返回参数**

NIMResult\<List\<String>> 取消订阅的用户账号列表

**相关回调**

无。

## <span id="queryUserStatusSubscriptions">queryUserStatusSubscriptions</span>

**接口描述**

查询用户状态订阅关系。

传入账号列表，查询自己订阅了其中哪些账号的状态，调用接口成功后，返回订阅的账号列表。

**参数说明**

```Dart
Future<NIMResult<List<NIMUserStatusSubscribeResult>>>
    queryUserStatusSubscriptions(List<String> accountIds) async {
  return _platform.queryUserStatusSubscriptions(accountIds);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `accountIds` | List\<String> | 否 | 需要查询的账号列表。查询自己是否订阅了对应账号。<li>如果账号列表为空，表示查询用户状态的所有订阅关系，即查询自己的全部订阅用户。<li>如果账号列表不为空，单次查询不超过 3000 个账号 ID，查询的数量越大查询时间越久，请尽量按需查询，建议限制在 100 个账号以内。 |

**示例代码**

```Dart
NimCore.instance.subscriptionService.queryUserStatusSubscriptions(accountIds).then((value){
       if(value.isSuccess){
         // 查询用户订阅状态成功
       }
    });
```

**返回参数**

NIMResult\<List\<[NIMUserStatusSubscribeResult](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMUserStatusSubscribeResult)>> 用户状态订阅关系查询结果

**相关回调**

无。

## <span id="publishCustomUserStatus">publishCustomUserStatus</span>

**接口描述**

发布用户自定义状态。

如果默认在线状态不满足业务需求，可以发布自定义用户状态。

**参数说明**

```Dart
Future<NIMResult<NIMCustomUserStatusPublishResult>> publishCustomUserStatus(
    NIMCustomUserStatusParams params) async {
  return _platform.publishCustomUserStatus(params);
}
```

| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `params` | [`NIMCustomUserStatusParams`](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMCustomUserStatusParams) | 是 | 发布自定义用户状态的配置参数。 |

**示例代码**

```Dart
NimCore.instance.subscriptionService.publishCustomUserStatus(params).then((value){
      if (value.isSuccess) {
        // 发布用户自定义状态成功
      }
    });
```

**返回参数**

NIMResult\<[NIMCustomUserStatusPublishResult](https://doc.yunxin.163.com/messaging2/client-apis/zExMjk2NzY?platform=client#NIMCustomUserStatusPublishResult)> 自定义用户状态发布结果

**相关回调**

无。