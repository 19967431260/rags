网易云信即时通讯 SDK（NetEase IM SDK，以下简称 NIM SDK）支持多种消息类型，助您快速实现多样化的消息业务场景。在进行消息收发等操作前，您需要先构建消息。

本文中消息指聊天室消息。更多聊天室消息相关功能请参考开发文档 [聊天室消息](https://doc.yunxin.163.com/messaging2/client-apis/DQzNjE0MDU?platform=client)。

## 支持平台

本文内容适用的开发平台或框架如下表所示：

Android | iOS | macOS/Windows | Web/uni-app/小程序 | Node.js/Electron | HarmonyOS |
:----: | :----: | :----: | :----: | :----: | :----: | 
✔️️ | ✔️️ | ✔️️ | ✔️️ | ✔️️ | ✔️️ |

## API 概览

API | 说明 | 起始版本
---- | ---- | ----
[createTextMessage](#createTextMessage) | 创建一条文本消息 | v10.2.0（对应 HarmonyOS v1.0.0）
[createImageMessage](#createImageMessage) | 创建一条图片消息 | v10.2.0（对应 HarmonyOS v1.0.0）
[createAudioMessage](#createAudioMessage) | 创建一条语音消息 | v10.2.0（对应 HarmonyOS v1.0.0）
[createVideoMessage](#createVideoMessage) | 创建一条视频消息 | v10.2.0（对应 HarmonyOS v1.0.0）
[createFileMessage](#createFileMessage) | 创建一条文件消息 | v10.2.0（对应 HarmonyOS v1.0.0）
[createLocationMessage](#createLocationMessage) | 创建一条地理位置消息 | v10.2.0（对应 HarmonyOS v1.0.0）
[createCustomMessage](#createCustomMessage) | 创建一条自定义消息 | v10.2.0（对应 HarmonyOS v1.0.0）
[createForwardMessage](#createForwardMessage) | 创建一条转发消息 | v10.2.0（对应 HarmonyOS v1.0.0）
[createTipsMessage](#createTipsMessage) | 创建一条提示消息 | v10.2.0（对应 HarmonyOS v1.0.0）
<!--未实现
[createCustomMessageWithAttachment](#createCustomMessageWithAttachment) | 构造自定义消息 | v10.5.0
-->

## 接口类

`V2NIMChatroomMessageCreator` 类提供聊天室消息构建接口，支持构建多种类型的聊天室消息。

## <span id="createTextMessage">createTextMessage</span>

**接口描述**

创建一条文本消息。

::: note note
- 在加入聊天室后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java
public static V2NIMChatroomMessage createTextMessage(String text)
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | String | 是 | 文本消息内容，长度上限为 5000 字符。 |
:::
::: code iOS
```Objective-C
+ (V2NIMChatroomMessage *)createTextMessage:(NSString *)text;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | NSString * | 是 | 文本消息内容，长度上限为 5000 字符。
:::
::: code macOS/Windows
```C++
static nstd::optional<V2NIMChatroomMessage> createTextMessage(nstd::string text);
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | nstd::string | 是 | 文本消息内容，长度上限为 5000 字符。
:::
::: code Web/uni-app/小程序
```TypeScript
createTextMessage(text: string): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | string | 是 | 文本消息内容，长度上限为 5000 字符。
:::
::: code Node.js/Electron
```TypeScript
createTextMessage(text): null | V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | string | 是 | 文本消息内容，长度上限为 5000 字符。
:::
::: code HarmonyOS
```TypeScript
createTextMessage(text: string): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | string | 是 | 文本消息内容，长度上限为 5000 字符。
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java
V2NIMChatroomMessage v2TextMessage = V2NIMChatroomMessageCreator.createTextMessage("text content");
```
:::
::: code iOS
```Objective-C
V2NIMChatroomMessage *v2TextMessage = [V2NIMChatroomMessageCreator createTextMessage:@"text content"];
```
:::
::: code macOS/Windows
```C++
auto textMessage = V2NIMChatroomMessageCreator::createTextMessage("hello world");
if(!textMessage) {
    // create text message failed
}
```
:::
::: code Web/uni-app/小程序
```TypeScript
try {
    const message = chatroom.V2NIMChatroomMessageCreator.createTextMessage('hello world')
} catch(err) {
    // todo error
}
```
:::
::: code Node.js/Electron
```TypeScript
try {
    const message = v2.chatroomMessageCreator.createTextMessage(text)
} catch(err) {
    // todo error
}
```
:::
::: code HarmonyOS
```TypeScript
const message: V2NIMChatroomMessage = this.chatroomClient.messageCreator.createTextMessage('hello world')
```
:::
::::::

**返回参数**

已创建的聊天室消息对象 [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage)

## <span id="createImageMessage">createImageMessage</span>

**接口描述**

创建一条图片消息。

::: note note
- 在加入聊天室后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java
public static V2NIMChatroomMessage createImageMessage(String imagePath, String name, String sceneName, Integer width, Integer height)
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `imagePath` | String | 是 | 图片 URL 地址。 |
| `name` | String | 否 | 图片文件的显示名称，可不同于文件名。 |
| `sceneName` | String | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `width` | Integer | 否 | 图片宽度，单位为像素。 |
| `height` | Integer | 否 | 图片高度，单位为像素。 |
:::
::: code iOS
```Objective-C
+ (V2NIMChatroomMessage *)createImageMessage:(NSString *)imagePath
                                name:(nullable NSString *)name
                           sceneName:(nullable NSString *)sceneName
                               width:(int)width
                              height:(int)height;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `imagePath` | NSString * | 是 | 图片 URL 地址。 |
| `name` | NSString * | 否 | 图片文件的显示名称，可不同于文件名。 |
| `sceneName` | NSString * | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `width` | int | 否 | 图片宽度，单位为像素。 |
| `height` | int | 否 | 图片高度，单位为像素。 |
:::
::: code macOS/Windows
```C++
static nstd::optional<V2NIMChatroomMessage> createImageMessage(nstd::string imagePath,
    nstd::string name,
    nstd::string sceneName,
    uint32_t width,
    uint32_t height);
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `imagePath` | nstd::string | 是 | 图片 URL 地址。 |
| `name` | nstd::string | 否 | 图片文件的显示名称，可不同于文件名。 |
| `sceneName` | nstd::string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `width` | uint32_t | 否 | 图片宽度，单位为像素。 |
| `height` | uint32_t | 否 | 图片高度，单位为像素。 |
:::
::: code Web/uni-app/小程序
```TypeScript
createImageMessage(imageObj: string | File, name?: string, sceneName?: string, width?: number, height?: number): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `imageObj` | Web File/string | 是 | <li>非 Web 小程序：Web File 对象<li>Web 小程序：图片 URL 地址。 |
| `name` | string | 否 | 图片文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `width` | number | 否 | 图片宽度，单位为像素。 |
| `height` | number | 否 | 图片高度，单位为像素。 |
:::
::: code Node.js/Electron
```TypeScript
createImageMessage(imagePath, name, sceneName, width, height): null | V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `imagePath` | String | 是 | 图片 URL 地址。 |
| `name` | string | 否 | 图片文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `width` | number | 否 | 图片宽度，单位为像素。 |
| `height` | number | 否 | 图片高度，单位为像素。 |
:::
::: code HarmonyOS
```TypeScript
createImageMessage(imagePath: string, name?: string, sceneName?: string, width?: number, height?: number): Promise<V2NIMChatroomMessage>
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `imagePath` | string | 是 | 图片 URL 地址。 |
| `name` | string | 否 | 图片文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `width` | number | 否 | 图片宽度，单位为像素。 |
| `height` | number | 否 | 图片高度，单位为像素。 |
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java
V2NIMChatroomMessage v2ImageMessage = V2NIMChatroomMessageCreator.createImageMessage(imagePath, name, sceneName, width, height);
```
:::
::: code iOS
```Objective-C
V2NIMChatroomMessage *v2ImageMessage = [V2NIMChatroomMessageCreator createImageMessage:imagePath
                                                                                 name:name
                                                                            sceneName:sceneName
                                                                                width:width
                                                                               height:height];
```
:::
::: code macOS/Windows
```C++
auto imageMessage = V2NIMChatroomMessageCreator::createImageMessage("imagePath", "imageName", V2NIM_STORAGE_SCENE_NAME_DEFAULT_IM, 100, 100);
if (!imageMessage) {
    // create image message failed
}
```
:::
::: code Web/uni-app/小程序
```TypeScript
/**
 * 除 imageObj 外，其它参数均是可选参数
 *
 * 浏览器中，imageObj 为 file 对象。小程序中，imageObj 为文件路径
 */
const message = chatroom.V2NIMChatroomMessageCreator.createImageMessage(imageObj, name, sceneName, width, height)
```
:::
::: code Node.js/Electron
```TypeScript
/**
 * 除 imagePath 外，其它参数均是可选参数
 *
 */
const message = v2.chatroomMessageCreator.createImageMessage(imagePath, name, sceneName, width, height)
```
:::
::: code HarmonyOS
```TypeScript
const message: V2NIMChatroomMessage = await this.chatroomClient.messageCreator.createImageMessage(imagePath, name, sceneName, width, height)
```
:::
::::::

**返回参数**

已创建的聊天室消息对象 [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage)

## <span id="createAudioMessage">createAudioMessage</span>

**接口描述**

创建一条语音消息。

::: note note
- 在加入聊天室后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java
public static V2NIMChatroomMessage createAudioMessage(String audioPath, String name, String sceneName, Integer duration)
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `audioPath` | String | 是 | 语音文件 URL 地址。 |
| `name` | String | 否 | 语音文件的显示名称，可不同于文件名。 |
| `sceneName` | String | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | Integer | 是 | 语音文件时长，单位为毫秒。 |
:::
::: code iOS
```Objective-C
+ (V2NIMChatroomMessage *)createAudioMessage:(NSString *)audioPath
                                name:(nullable NSString *)name
                           sceneName:(nullable NSString *)sceneName
                            duration:(int)duration;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `audioPath` | NSString * | 是 | 语音文件 URL 地址。 |
| `name` | NSString * | 否 | 语音文件的显示名称，可不同于文件名。 |
| `sceneName` | NSString * | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | int | 是 | 语音文件时长，单位为毫秒。 |
:::
::: code macOS/Windows
```C++
static nstd::optional<V2NIMChatroomMessage> createAudioMessage(nstd::string audioPath,
    nstd::string name,
    nstd::string sceneName,
    uint32_t duration);
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `audioPath` | nstd::string | 是 | 语音文件 URL 地址。 |
| `name` | nstd::string | 否 | 语音文件的显示名称，可不同于文件名。 |
| `sceneName` | nstd::string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | uint32_t | 是 | 语音文件时长，单位为毫秒。 |
:::
::: code Web/uni-app/小程序
```TypeScript
createAudioMessage(audioObj: string | File, name?: string, sceneName?: string, duration?: number): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `audioObj` | Web File/string | 是 | <li>非 Web 小程序：Web File 对象<li>Web 小程序：语音文件 URL 地址。 |
| `name` | string | 否 | 语音文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | number | 是 | 语音文件时长，单位为毫秒。 |
:::
::: code Node.js/Electron
```TypeScript
createAudioMessage(audioPath, name, sceneName, duration): null | V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `audioPath` | string | 是 | 语音文件 URL 地址。 |
| `name` | string | 否 | 语音文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | number | 是 | 语音文件时长，单位为毫秒。 |
:::
::: code HarmonyOS
```TypeScript
createAudioMessage(audioPath: string, name?: string, sceneName?: string, duration?: number): Promise<V2NIMChatroomMessage>
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `audioPath` | string | 是 | 语音文件 URL 地址。 |
| `name` | string | 否 | 语音文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | number | 是 | 语音文件时长，单位为毫秒。 |
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java
V2NIMChatroomMessage v2AudioMessage = V2NIMChatroomMessageCreator.createAudioMessage(audioPath, name, sceneName, duration);
```
:::
::: code iOS
```Objective-C
V2NIMChatroomMessage *v2AudioMessage = [V2NIMChatroomMessageCreator createAudioMessage:audioPath
                                                                                  name:name
                                                                             sceneName:sceneName
                                                                              duration:duration];
```
:::
::: code macOS/Windows
```C++
auto audioMessage = V2NIMChatroomMessageCreator::createAudioMessage("audioPath", "audioName", V2NIM_STORAGE_SCENE_NAME_DEFAULT_IM, 100);
if(!audioMessage) {
    // create audio message failed
}
```
:::
::: code Web/uni-app/小程序
```TypeScript
const message = chatroom.V2NIMChatroomMessageCreator.createAudioMessage(audioObj, name, sceneName, duration)
```
:::
::: code Node.js/Electron
```TypeScript
const message = v2.chatroomMessageCreator.createAudioMessage(audioPath, name, sceneName, duration)
```
:::
::: code HarmonyOS
```TypeScript
const message: V2NIMChatroomMessage = await this.chatroomClient.messageCreator.createAudioMessage(audioPath, name, sceneName, duration)
```
:::
::::::

**返回参数**

已创建的聊天室消息对象 [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage)

## <span id="createVideoMessage">createVideoMessage</span>

**接口描述**

创建一条视频消息。

::: note note
- 在加入聊天室后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java
public static V2NIMChatroomMessage createVideoMessage(String videoPath, String name, String sceneName, Integer duration, Integer width, Integer height)
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `videoPath` | String | 是 | 视频文件 URL 地址。 |
| `name` | String | 否 | 视频文件的显示名称，可不同于文件名。 |
| `sceneName` | String | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | Integer | 是 | 视频文件时长，单位为毫秒。 |
| `width` | Integer | 否 | 视频宽度，单位为像素。 |
| `height` | Integer | 否 | 视频高度，单位为像素。 |
:::
::: code iOS
```Objective-C
+ (V2NIMChatroomMessage *)createVideoMessage:(NSString *)videoPath
                                name:(nullable NSString *)name
                           sceneName:(nullable NSString *)sceneName
                            duration:(int)duration
                               width:(int)width
                              height:(int)height;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `videoPath` | NSString * | 是 | 视频文件 URL 地址。 |
| `name` | NSString * | 否 | 视频文件的显示名称，可不同于文件名。 |
| `sceneName` | NSString * | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | int | 是 | 视频文件时长，单位为毫秒。 |
| `width` | int | 否 | 视频宽度，单位为像素。 |
| `height` | int | 否 | 视频高度，单位为像素。 |
:::
::: code macOS/Windows
```C++
static nstd::optional<V2NIMChatroomMessage> createVideoMessage(nstd::string videoPath,
    nstd::string name,
    nstd::string sceneName,
    uint32_t duration,
    uint32_t width,
    uint32_t height);
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `videoPath` | nstd::string | 是 | 视频文件 URL 地址。 |
| `name` | nstd::string | 否 | 视频文件的显示名称，可不同于文件名。 |
| `sceneName` | nstd::string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | uint32_t | 是 | 视频文件时长，单位为毫秒。 |
| `width` | uint32_t | 否 | 视频宽度，单位为像素。 |
| `height` | uint32_t | 否 | 视频高度，单位为像素。 |
:::
::: code Web/uni-app/小程序
```TypeScript
createVideoMessage(videoObj: string | File, name?: string, sceneName?: string, duration?: number, width?: number, height?: number): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `videoObj` | Web File/string | 是 | <li>非 Web 小程序：Web File 对象<li>Web 小程序：视频文件 URL 地址。 |
| `name` | string | 否 | 视频文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | number | 是 | 视频文件时长，单位为毫秒。 |
| `width` | number | 否 | 视频宽度，单位为像素。 |
| `height` | number | 否 | 视频高度，单位为像素。 |
:::
::: code Node.js/Electron
```TypeScript
createVideoMessage(videoPath, name, sceneName, duration, width, height): null | V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `videoPath` | string | 是 | 视频文件 URL 地址。 |
| `name` | string | 否 | 视频文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | number | 是 | 视频文件时长，单位为毫秒。 |
| `width` | number | 否 | 视频宽度，单位为像素。 |
| `height` | number | 否 | 视频高度，单位为像素。 |
:::
::: code HarmonyOS
```TypeScript
createVideoMessage(videoPath: string, name?: string, sceneName?: string, duration?: number, width?: number, height?: number): Promise<V2NIMChatroomMessage>
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `videoPath` | string | 是 | 视频文件 URL 地址。 |
| `name` | string | 否 | 视频文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
| `duration` | number | 是 | 视频文件时长，单位为毫秒。 |
| `width` | number | 否 | 视频宽度，单位为像素。 |
| `height` | number | 否 | 视频高度，单位为像素。 |
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java
V2NIMChatroomMessage v2VideoMessage = V2NIMChatroomMessageCreator.createVideoMessage(videoPath, name, sceneName, duration, width, height);
```
:::
::: code iOS
```Objective-C
V2NIMChatroomMessage *v2VideoMessage = [V2NIMChatroomMessageCreator createVideoMessage:videoPath
                                                                                  name:name
                                                                             sceneName:sceneName
                                                                              duration:duration
                                                                                 width:width
                                                                                height:height];
```
:::
::: code macOS/Windows
```C++
auto videoMessage = V2NIMChatroomMessageCreator::createVideoMessage("videoPath", "videoName", V2NIM_STORAGE_SCENE_NAME_DEFAULT_IM, 100, 100,
100); if (!videoMessage) {
    // create video message failed
}
```
:::
::: code Web/uni-app/小程序
```TypeScript
const message = chatroom.V2NIMChatroomMessageCreator.createVideoMessage(videoObj, name, sceneName, duration, width, height)
```
:::
::: code Node.js/Electron
```TypeScript
const message = v2.chatroomMessageCreator.createVideoMessage(videoPath, name, sceneName, duration, width, height)
```
:::
::: code HarmonyOS
```TypeScript
const message = await this.chatroomClient.messageCreator.createVideoMessage(videoPath, name, sceneName, duration, width, height)
```
:::
::::::

**返回参数**

已创建的聊天室消息对象 [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage)

## <span id="createFileMessage">createFileMessage</span>

**接口描述**

创建一条文件消息。

::: note note
- 在加入聊天室后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java
public static V2NIMChatroomMessage createFileMessage(String filePath, String name, String sceneName)
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `filePath` | String | 是 | 文件 URL 地址。 |
| `name` | String | 否 | 文件的显示名称，可不同于文件名。 |
| `sceneName` | String | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
:::
::: code iOS
```Objective-C
+ (V2NIMChatroomMessage *)createFileMessage:(NSString *)filePath
                               name:(nullable NSString *)name
                          sceneName:(nullable NSString *)sceneName;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `filePath` | NSString * | 是 | 文件 URL 地址。 |
| `name` | NSString * | 否 | 文件的显示名称，可不同于文件名。 |
| `sceneName` | NSString * | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
:::
::: code macOS/Windows
```C++
static nstd::optional<V2NIMChatroomMessage> createFileMessage(nstd::string filePath, nstd::string name, nstd::string sceneName);
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `filePath` | nstd::string | 是 | 文件 URL 地址。 |
| `name` | nstd::string | 否 | 文件的显示名称，可不同于文件名。 |
| `sceneName` | nstd::string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
:::
::: code Web/uni-app/小程序
```TypeScript
createFileMessage(fileObj: string | File, name?: string, sceneName?: string): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `fileObj` | Web File/string | 是 | <li>非 Web 小程序：Web File 对象<li>Web 小程序：文件 URL 地址。 |
| `name` | string | 否 | 文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
:::
::: code Node.js/Electron
```TypeScript
createFileMessage(filePath, name, sceneName): null | V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `filePath` | string | 是 | 文件 URL 地址。 |
| `name` | string | 否 | 文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
:::
::: code HarmonyOS
```TypeScript
createFileMessage(filePath: string, name?: string, sceneName?: string): Promise<V2NIMChatroomMessage>
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `filePath` | string | 是 | 文件 URL 地址。 |
| `name` | string | 否 | 文件的显示名称，可不同于文件名。 |
| `sceneName` | string | 是 | NOS 文件存储场景名，默认值为 `V2NIMStorageSceneConfig.DEFAULT_IM` 对应的场景名。若使用自定义的存储场景，需要先调用 [`addCustomStorageScene`](https://doc.yunxin.163.com/messaging2/client-apis/zQ0MDc5MjI?platform=client#addCustomStorageScene) 添加自定义存储场景。 |
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java
V2NIMChatroomMessage v2FileMessage = V2NIMChatroomMessageCreator.createFileMessage(filePath, name, sceneName);
```
:::
::: code iOS
```Objective-C
V2NIMChatroomMessage *v2FileMessage = [V2NIMChatroomMessageCreator createFileMessage:filePath
                                                                                name:name
                                                                           sceneName:sceneName];
```
:::
::: code macOS/Windows
```C++
auto fileMessage = V2NIMChatroomMessageCreator::createFileMessage("filePath", "fileName", V2NIM_STORAGE_SCENE_NAME_DEFAULT_IM);
if(!fileMessage) {
    // create file message failed
}
```
:::
::: code Web/uni-app/小程序
```TypeScript
const message = chatroom.V2NIMChatroomMessageCreator.createFileMessage(fileObj, name, sceneName)
```
:::
::: code Node.js/Electron
```TypeScript
const message = v2.chatroomMessageCreator.createFileMessage(filePath, name, sceneName)
```
:::
::: code HarmonyOS
```TypeScript
const message = await this.chatroomClient.messageCreator.createFileMessage(filePath, name, sceneName)
```
:::
::::::

**返回参数**

已创建的聊天室消息对象 [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage)

## <span id="createLocationMessage">createLocationMessage</span>

**接口描述**

创建一条地理位置消息。

::: note note
- 在加入聊天室后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java
public static V2NIMChatroomMessage createLocationMessage(double latitude, double longitude, String address)
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `latitude` | double | 是 | 位置维度。 |
| `longitude` | double | 是 | 位置经度。 |
| `address` | String | 是 | 位置描述信息。 |
:::
::: code iOS
```Objective-C
+ (V2NIMChatroomMessage *)createLocationMessage:(double)latitude
                              longitude:(double)longitude
                                address:(NSString *)address;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `latitude` | double | 是 | 位置维度。 |
| `longitude` | double | 是 | 位置经度。 |
| `address` | NSString * | 是 | 位置描述信息。 |
:::
::: code macOS/Windows
```C++
static nstd::optional<V2NIMChatroomMessage> createLocationMessage(double latitude, double longitude, nstd::string address);
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `latitude` | double | 是 | 位置维度。 |
| `longitude` | double | 是 | 位置经度。 |
| `address` | nstd::string | 是 | 位置描述信息。 |
:::
::: code Web/uni-app/小程序
```TypeScript
createLocationMessage(latitude: number, longitude: number, address: string): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `latitude` | number | 是 | 位置维度。 |
| `longitude` | number | 是 | 位置经度。 |
| `address` | string | 是 | 位置描述信息。 |
:::
::: code Node.js/Electron
```TypeScript
createLocationMessage(latitude, longitude, address): null | V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `latitude` | number | 是 | 位置维度。 |
| `longitude` | number | 是 | 位置经度。 |
| `address` | string | 是 | 位置描述信息。 |
:::
::: code HarmonyOS
```TypeScript
createLocationMessage(latitude: number, longitude: number, address: string): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `latitude` | number | 是 | 位置维度。 |
| `longitude` | number | 是 | 位置经度。 |
| `address` | string | 是 | 位置描述信息。 |
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java
V2NIMChatroomMessage v2LocationMessage = V2NIMChatroomMessageCreator.createLocationMessage(latitude,longitude, address);
```
:::
::: code iOS
```Objective-C
V2NIMChatroomMessage *v2LocationMessage = [V2NIMChatroomMessageCreator createLocationMessage:latitude
                                                                                   longitude:longitude
                                                                                     address:address];
```
:::
::: code macOS/Windows
```C++
auto locationMessage = V2NIMChatroomMessageCreator::createLocationMessage(100, 100, "address");
if(!locationMessage) {
    // create location message failed
}
```
:::
::: code Web/uni-app/小程序
```TypeScript
const message = chatroom.V2NIMChatroomMessageCreator.createLocationMessage(30.25, 120.166664, "HangZhou")
```
:::
::: code Node.js/Electron
```TypeScript
const message = v2.chatroomMessageCreator.createLocationMessage(30.25, 120.166664, "HangZhou")
```
:::
::: code HarmonyOS
```TypeScript
const message = this.chatroomClient.messageCreator.createLocationMessage(30.25, 120.166664, "HangZhou")
```
:::
::::::

**返回参数**

已创建的聊天室消息对象 [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage)

## <span id="createCustomMessage">createCustomMessage</span>

**接口描述**

创建一条自定义消息。

::: note note
- 由于 NIM SDK 并不知道自定义消息的格式，您的应用需要注册一个自定义消息解析器。请参考 [聊天室自定义消息收发](https://doc.yunxin.163.com/messaging2/client-apis/DY3Mjg5NjE?platform=android)。
- 在加入聊天室后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java
public static V2NIMChatroomMessage createCustomMessage(String rawAttachment)
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `rawAttachment` | String | 是 | 自定义消息附件对象。SDK 会将 JSON 格式的自定义消息解析为附件对象，用于自定义消息收发。长度限制与各套餐的配置（消息 attach 字段大小上限）有关，具体请参考 [字段限制说明](https://doc.yunxin.163.com/messaging2/faq/zc1MzA1MDY?platform=client)。 |
:::
::: code iOS
```Objective-C
+ (V2NIMChatroomMessage *)createCustomMessage:(NSString *)rawAttachment;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `rawAttachment` | NSString * | 是 | 自定义消息附件对象。SDK 会将 JSON 格式的自定义消息解析为附件对象，用于自定义消息收发。长度限制与各套餐的配置（消息 attach 字段大小上限）有关，具体请参考 [字段限制说明](https://doc.yunxin.163.com/messaging2/faq/zc1MzA1MDY?platform=client)。 |
:::
::: code macOS/Windows
```C++
static nstd::optional<V2NIMChatroomMessage> createCustomMessage(nstd::string rawAttachment);
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `rawAttachment` | nstd::string | 是 | 自定义消息附件对象。SDK 会将 JSON 格式的自定义消息解析为附件对象，用于自定义消息收发。长度限制与各套餐的配置（消息 attach 字段大小上限）有关，具体请参考 [字段限制说明](https://doc.yunxin.163.com/messaging2/faq/zc1MzA1MDY?platform=client)。 |
:::
::: code Web/uni-app/小程序
```TypeScript
createCustomMessage(rawAttachment: string): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `rawAttachment` | string | 是 | 自定义消息附件对象。SDK 会将 JSON 格式的自定义消息解析为附件对象，用于自定义消息收发。长度限制与各套餐的配置（消息 attach 字段大小上限）有关，具体请参考 [字段限制说明](https://doc.yunxin.163.com/messaging2/faq/zc1MzA1MDY?platform=client)。 |
:::
::: code Node.js/Electron
```TypeScript
createCustomMessage(rawAttachment): null | V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `rawAttachment` | string | 是 | 自定义消息附件对象。SDK 会将 JSON 格式的自定义消息解析为附件对象，用于自定义消息收发。长度限制与各套餐的配置（消息 attach 字段大小上限）有关，具体请参考 [字段限制说明](https://doc.yunxin.163.com/messaging2/faq/zc1MzA1MDY?platform=client)。 |
:::
::: code HarmonyOS
```TypeScript
createCustomMessage(rawAttachment: string): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `rawAttachment` | string | 是 | 自定义消息附件对象。SDK 会将 JSON 格式的自定义消息解析为附件对象，用于自定义消息收发。长度限制与各套餐的配置（消息 attach 字段大小上限）有关，具体请参考 [字段限制说明](https://doc.yunxin.163.com/messaging2/faq/zc1MzA1MDY?platform=client)。 |
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java
V2NIMChatroomMessage v2CustomMessage = V2NIMChatroomMessageCreator.createCustomMessage(rawAttachment);
```
:::
::: code iOS
```Objective-C
V2NIMChatroomMessage *v2CustomMessage = [V2NIMChatroomMessageCreator createCustomMessage:rawAttachment];
```
:::
::: code macOS/Windows
```C++
auto customMessage = V2NIMChatroomMessageCreator::createCustomMessage(R"({"key": "value"})");
if(!customMessage) {
    // create custom message failed
}
```
:::
::: code Web/uni-app/小程序
```TypeScript
const message = chatroom.V2NIMChatroomMessageCreator.createCustomMessage(JSON.stringify({
    strategy: 1
}))
```
:::
::: code Node.js/Electron
```TypeScript
const message = v2.chatroomMessageCreator.createCustomMessage(rawAttachment)
```
:::
::: code HarmonyOS
```TypeScript
createCustomMessage(rawAttachment: string): V2NIMChatroomMessage
```
:::
::::::

**返回参数**

已创建的聊天室消息对象 [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage)

## <span id="createForwardMessage">createForwardMessage</span>

**接口描述**

创建一条转发消息。

::: note note
- 转发的消息类型 **不能为** `V2NIM_MESSAGE_TYPE_NOTIFICATION(5)`、`V2NIM_MESSAGE_TYPE_ROBOT(11)`、`V2NIM_MESSAGE_TYPE_TIPS(10)`，`V2NIM_MESSAGE_TYPE_AVCHAT(7)`。
- 转发的消息消息必须为发送成功的消息，消息状态必须为 `V2NIMMessagesendingState.V2NIM_MESSAGE_SENDING_STATE_SUCCEEDED(1)`，消息内容与原消息相同。
- 在加入聊天室后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java
public static V2NIMChatroomMessage createForwardMessage(V2NIMChatroomMessage message)
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `message` | [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage) | 是 | 待转发的消息体。 |
:::
::: code iOS
```Objective-C
+ (V2NIMChatroomMessage *)createForwardMessage:(V2NIMChatroomMessage *)message;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `message` | [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage) | 是 | 待转发的消息体。 |
:::
::: code macOS/Windows
```C++
static nstd::optional<V2NIMChatroomMessage> createForwardMessage(V2NIMChatroomMessage message);
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `message` | [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage) | 是 | 待转发的消息体。 |
:::
::: code Web/uni-app/小程序
```TypeScript
createForwardMessage(message: V2NIMChatroomMessage): V2NIMChatroomMessage | null
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `message` | [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage) | 是 | 待转发的消息体。 |
:::
::: code Node.js/Electron
```TypeScript
createForwardMessage(message): null | V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `message` | [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage) | 是 | 待转发的消息体。 |
:::
::: code HarmonyOS
```TypeScript
createForwardMessage(message: V2NIMChatroomMessage): V2NIMChatroomMessage | null
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `message` | [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage) | 是 | 待转发的消息体。 |
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java
V2NIMChatroomMessage v2ForwardMessage = V2NIMChatroomMessageCreator.createForwardMessage(v2ChatroomMessage);
```
:::
::: code iOS
```Objective-C
V2NIMChatroomMessage *v2ForwardMessage = [V2NIMChatroomMessageCreator createForwardMessage:v2ChatroomMessage];
```
:::
::: code macOS/Windows
```C++
auto forwardMessage = V2NIMChatroomMessageCreator::createForwardMessage(message);
if(!forwardMessage) {
    // create forward message failed
}
```
:::
::: code Web/uni-app/小程序
```TypeScript
const newMessage = chatroom.V2NIMChatroomMessageCreator.createForwardMessage(message)
```
:::
::: code Node.js/Electron
```TypeScript
const message = v2.chatroomMessageCreator.createForwardMessage(message)
```
:::
::: code HarmonyOS
```TypeScript
const newMessage = this.chatroomClient.messageCreator.createForwardMessage(message)
```
:::
::::::

**返回参数**

已创建的聊天室消息对象 [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage)

## <span id="createTipsMessage">createTipsMessage</span>

**接口描述**

创建一条提示消息。

::: note note
- 在加入聊天室后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java
public static V2NIMChatroomMessage createTipsMessage(String text)
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | String | 是 | 提示消息内容。 |
:::
::: code iOS
```Objective-C
+ (V2NIMChatroomMessage *)createTipsMessage:(NSString *)text;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | NSString * | 是 | 提示消息内容。 |
:::
::: code macOS/Windows
```C++
static nstd::optional<V2NIMChatroomMessage> createTipsMessage(nstd::string text);
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | nstd::string | 是 | 提示消息内容。 |
:::
::: code Web/uni-app/小程序
```TypeScript
createTipsMessage(text: string): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | string | 是 | 提示消息内容。 |
:::
::: code Node.js/Electron
```TypeScript
createTipsMessage(text): null | V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | string | 是 | 提示消息内容。 |
:::
::: code HarmonyOS
```TypeScript
createTipsMessage(text: string): V2NIMChatroomMessage
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `text` | string | 是 | 提示消息内容。 |
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java
V2NIMChatroomMessage v2TipMessage = V2NIMChatroomMessageCreator.createTipsMessage("tip content");
```
:::
::: code iOS
```Objective-C
V2NIMChatroomMessage *v2TipMessage = [V2NIMChatroomMessageCreator createTipsMessage:@"tip content"];
```
:::
::: code macOS/Windows
```C++
auto tipMessage = V2NIMChatroomMessageCreator::createTipsMessage("text");
if(!tipMessage) {
    // create tip message failed
}
```
:::
::: code Web/uni-app/小程序
```TypeScript
const message = chatroom.V2NIMChatroomMessageCreator.createTipsMessage('hint')
```
:::
::: code Node.js/Electron
```TypeScript
const message = v2.chatroomMessageCreator.createTipsMessage(text)
```
:::
::: code HarmonyOS
```TypeScript
const message: V2NIMChatroomMessage = this.chatroomClient.messageCreator.createTipsMessage('hint')
```
:::
::::::

**返回参数**

已创建的聊天室消息对象 [`V2NIMChatroomMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMChatroomMessage)

<!--未实现
## <span id="createCustomMessageWithAttachment">createCustomMessageWithAttachment</span>

**接口描述**

构造一条自定义消息。

::: note note
- 在登录后、发送消息前调用该方法。
- 该方法为同步。
:::

**参数说明**

:::::: div linked-codes
::: code Android
```Java

```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `attachment` | [V2NIMMessageCustomAttachment](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMMessageCustomAttachment) | 是 | 需要发送的自定义消息的附件。 |
| `subType` | int | 否 | 可作为具体的自定义子类型区分 type 字段。需要传入大于等于 0 的值，若小于 0 ，则默认为 0。 |
:::
::: code iOS
```Objective-C

```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `attachment` | [V2NIMMessageCustomAttachment](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMMessageCustomAttachment) | 是 | 需要发送的自定义消息的附件。 |
| `subType` | int | 否 | 可作为具体的自定义子类型区分 type 字段。需要传入大于等于 0 的值，若小于 0 ，则默认为 0。 |
:::
::: code macOS/Windows
```C++
virtual nstd::optional<V2NIMMessage> createCustomMessageWithAttachment(nstd::shared_ptr<V2NIMMessageCustomAttachment> attachment,
        int32_t subType) = 0;
```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `attachment` | nstd::shared_ptr<[V2NIMMessageCustomAttachment](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMMessageCustomAttachment)> | 是 | 需要发送的自定义消息的附件。 |
| `subType` | int32_t | 否 | 可作为具体的自定义子类型区分 type 字段。需要传入大于等于 0 的值，若小于 0 ，则默认为 0。 |
:::
::: code Web/uni-app/小程序
```TypeScript

```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `attachment` | [V2NIMMessageCustomAttachment](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMMessageCustomAttachment) | 是 | 需要发送的自定义消息的附件。 |
| `subType` | int | 否 | 可作为具体的自定义子类型区分 type 字段。需要传入大于等于 0 的值，若小于 0 ，则默认为 0。 |
:::
::: code Node.js/Electron
```TypeScript

```
| 参数名称 | 类型 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `attachment` | [V2NIMMessageCustomAttachment](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMMessageCustomAttachment) | 是 | 需要发送的自定义消息的附件。 |
| `subType` | int | 否 | 可作为具体的自定义子类型区分 type 字段。需要传入大于等于 0 的值，若小于 0 ，则默认为 0。 |
:::
::::::

**示例代码**

:::::: div linked-codes
::: code Android
```Java

```
:::
::: code iOS
```Objective-C

```
:::
::: code macOS/Windows
```C++
struct V2NIMMessageTempCustomAttachment : public V2NIMMessageCustomAttachment {
     nstd::shared_ptr<V2NIMMessageAttachment> parse(const nstd::string& attach) override {
        // parse attach
    }
}
nstd::shared_ptr<V2NIMMessageTempCustomAttachment> attachment = nstd::make_shared<V2NIMMessageTempCustomAttachment>();
int32_t subType = 1;
auto customMessage = V2NIMChatroomMessageCreator::createCustomMessageWithAttachment(attachment, subType);
```
:::
::: code Web/uni-app/小程序
```TypeScript

```
:::
::: code Node.js/Electron
```TypeScript

```
:::
::::::

**返回参数**

已创建的自定义消息对象 [`V2NIMMessage`](https://doc.yunxin.163.com/messaging2/client-apis/DAxNjk0Mzc?platform=client#V2NIMMessage)
-->