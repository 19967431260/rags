
网易云信音视频中的 UID 是完全自定义的用户 ID，因为用户 ID 不一定是唯一的，所以 UID 可能会重复使用。

**常规使用场景：**

同时间仅有一个设备，使用同一个 UID，仅加入一个房间。在退出房间后再重复使用。

**重复使用场景：**

同时间有多个设备，使用同一个 UID。

此场景下需要注意：

- 加入同一个房间，后进房间的设备会把先进房间的设备踢掉。
- 加入不同房间，多个设备在不同房间均可正常通话。