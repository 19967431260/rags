该接口允许您对已创建的云端录制任务进行自定义布局参数的动态更新，适用于实时调整录制内容的显示方式。对于已经 [创建云端录制任务](https://doc.yunxin.163.com/nertc/server-apis/TEyOTk2OTY?platform=server) 的音视频通话，支持根据需要动态修改录制布局设置。

## 接口说明

此接口适用于以下场景：

- 当有用户加入或离开音视频房间时，及时调整视频窗口布局。
- 当有用户开始或结束屏幕共享时，调整录制画面的布局。
- 需要在录制过程中调整视频窗口大小和位置。
- 在录制过程中需要调整录制的用户名单。

对于已经通过 [创建云端录制任务](https://doc.yunxin.163.com/nertc/server-apis/TEyOTk2OTY?platform=server) 接口启动的音视频通话录制，更新录制布局时，您只需要在本接口请求体中修改需要更新的布局参数，其余参数保持原云端录制任务的设置（可通过 [查询云端录制配置](https://doc.yunxin.163.com/nertc/server-apis/TU4ODczNDk?platform=server) 接口查询）：

- **布局配置**：

    - 更新 `layoutConfig.layout` 中的 `users`（用户视频窗口）、`subStreams`（屏幕共享窗口）、`images`（图片窗口）的布局位置和大小。
    - 更新 `recordConfig.modeList[].layout` 中的 `users`、`subStreams`、`images` 的布局位置和大小。

- **流订阅配置**：更新 `streamSubscribe` 参数，动态调整订阅哪些用户的音频流或视频流。

- **水印配置**：更新 `watermark` 和 `recordConfig.modeList[].watermark` 参数，动态调整图片、文字、透明层或者时间戳水印。

有关录制布局参数的详细说明，请参考《录制布局》[布局参数说明](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#layout) 章节和 [更新录制布局](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#update) 章节。

## 接口限制

默认上限为 50 次/秒，若请求频率超出限制，可能会返回 429 错误码。若您需要上调上限，请参考 [如何处理调用服务端 RESTful API 超出频率限制](https://doc.yunxin.163.com/nertc/server-apis/Tg3MDU1NTE?platform=server#如何处理调用服务端-restful-api-超出频率限制)。

## 请求信息

### 请求 URL

<style>
table th:first-of-type {    width: 15%;}
table th:nth-of-type(2) {    width: 10%;}
table th:nth-of-type(3) {    width: 10%;}
table th:nth-of-type(4) {    width: 55%;}
</style>

- **请求方法**：POST
- **请求 URL**：网易云信为该功能提供以下两个 API 请求地址，使用 V3 地址需在 URL 中指定 cname，您可以根据业务需求调用任一接口。

    - `https://rtc.yunxinapi.com/v2/api/cloudrecord/update`
    - `https://rtc.yunxinapi.com/v3/api/cloudrecord/update?cname={cname}`

### 请求路径参数

URL 中参数说明：

| 参数名称 | 类型 | 示例 | 说明 |
| ---- | ---- | ---- | ---- |
| cname | String | abc | 房间名称。仅在调用 V3 接口时需要设置。 |

### 请求头参数

请求中 Header 的设置请参考 [请求结构](https://doc.yunxin.163.com/nertc/server-apis/TYyNjA3MzQ?platform=server#%E8%AF%B7%E6%B1%82%E7%BB%93%E6%9E%84) 描述。

<!-- 
### 请求体参数(IM格式)

参数名称 | 类型 | 是否必选 | 说明 |
---- | ---- | ---- | ---- |
cid | Number | 必选，仅适用于 V2 接口 | 房间 ID。该 ID 为创建房间接口调用成功后返回的房间 ID。仅在调用 V2 接口时需要设置。 |
taskId | String | 是 | 任务唯一标识符，房间内唯一。调用 [创建云端录制任务](https://doc.yunxin.163.com/nertc/server-apis/TEyOTk2OTY?platform=server) 接口后，可在响应体中获取 taskId。 |
recordConfig | Object | 否 | 录制模式。详细信息请参考 [recordConfig](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#recordConfig)。<br> 支持如下三种模式：<ul> <li> **合流+单流录制（默认）**。房间内所有或指定 UID 的媒体流混合录制为一个媒体文件，同时每个 UID 均有其对应的媒体文件。<li> **合流录制**。房间内所有或指定 UID 的媒体流混合录制为一个媒体文件。<li> **单流录制**：分开录制房间内每个 UID 的媒体流，每个 UID 均有其对应的媒体文件。<br><note type="note">请在开始录制的时候设置录制模式，开始录制后，不支持切换录制模式。</note> |
  recordType | Number | 否 | 录制类型 |
  modeList | Array | 否 | 录制模式列表 |
    mode | Number | 是 | 录制模式，如示例中的100和1 |
    layout | Object | 否 | 布局配置，对应mode为1时 |
      canvas | Object | 否 | 画布配置 |
         height | Number | 否 | 画布高度 |
         width | Number | 否 | 画布宽度 |
      <font color="red">subStreams | Array | 否 | 辅流配置数组 |
         adaption | Number | 否 | 自适应方式 |
         height | Number | 否 | 高度 |
         width | Number | 否 | 宽度 |
         x | Number | 否 | x坐标 |
         y | Number | 否 | y坐标 |
      <font color="red">users</font> | Array | 否 | 用户配置数组 |
         adaption | Number | 否 | 自适应方式 |
         height | Number | 否 | 高度 |
         width | Number | 否 | 宽度 |
         x | Number | 否 | x坐标 |
         y | Number | 否 | y坐标 |
streamSubscribe | Object | 否 | 在录制过程中，通过黑白名单指定订阅某些用户的音频流或视频流。具体请参考 [订阅名单](https://doc.yunxin.163.com/nertc/server-apis/jQwNDAxNDc?platform=server)。<br>默认为订阅房间内所有发布的音视频流。 |
  streamType | Number | 否 | 流类型，示例中为2 |
  audioUidList | Object | 否 | 音频订阅配置 |
    unSubscribeUids | Array | 否 | 不订阅的音频用户ID列表 |
  videoUidList | Object | 否 | 视频订阅配置 |
    subscribeUids | Array | 否 | 订阅的视频用户ID列表 |
detect | Object | 否 | 内容安全审核配置。详细信息请参考 [detect](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#detect)。 |
  enableSpamDetect | Boolean | 否 | 是否启用安全审核 |
  scFrequency | Number | 否 | 安全审核频率 |
  detectType | Number | 否 | 审核类型 |
layoutConfig | Object | 否 | 录制布局配置。详细信息请参考 [录制布局](https://doc.yunxin.163.com/nertc/server-apis/TA0MjE1MzE?platform=server)。<br>录制布局包括以下几种模式：<br>- 画廊模式<br>- 主讲人模式<br>- 自定义布局<br>- 悬浮布局<br>- 垂直布局 |
  hostUid | Number | 否 | 主持人用户ID |
  layoutType | Number | 否 | 布局类型，示例中为 3 |
watermark | Object | 否 | 水印配置。详细信息请参考 [watermark](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#watermark)。<note type="note">该功能只针对合流录制模式的录制文件生效。</note> |
  literaWms | Array | 否 | 文字水印配置数组 |
    wmLitera | String | 否 | 水印文字内容 |
    fontSize | Number | 否 | 字体大小 |
    fontColor | String | 否 | 字体颜色，格式为"#RRGGBB" |
    offsetX | Number | 否 | X轴偏移 |
    offsetY | Number | 否 | Y轴偏移 |
  transparentLayers | Array | 否 | 透明图层配置数组 |
    offsetX | Number | 否 | X轴偏移 |
    offsetY | Number | 否 | Y轴偏移 |
    wmWidth | Number | 否 | 宽度 |
    wmHeight | Number | 否 | 高度 |
    bgTransparency | Number | 否 | 背景透明度 |
  imgWms | Array | 否 | 图片水印配置数组 |
    url | String | 否 | 图片URL |
    offsetX | Number | 否 | X轴偏移 |
    offsetY | Number | 否 | Y轴偏移 |
    wmWidth | Number | 否 | 宽度 |
    wmHeight | Number | 否 | 高度 |
  timestampWm | Object | 否 | 时间戳水印配置 |
    fontSize | Number | 否 | 字体大小 |
    fontColor | String | 否 | 字体颜色，格式为"#RRGGBB" |
    offsetX | Number | 否 | X轴偏移 |
    offsetY | Number | 否 | Y轴偏移 |
storageConfig | Object | 否 | 云存储配置信息 |
  bucket | String | 是 | 存储桶名称 |
  accessKey | String | 是 | 访问密钥ID |
  secretKey | String | 是 | 访问密钥密码 |
  region | String | 是 | 存储桶区域，例如：ap-southeast-1 |
  storageProvider | Number | 是 | 存储厂商类型，可选值：<br>0："nos"(网易云)<br>1："ali"(阿里云OSS)<br>2："cos"(腾讯云COS)<br>3："s3"(AWS S3) |
  filePrefix | String | 否 | 对象存储前缀，例如："dir/" | -->

### 请求体参数

参数名称 | 类型 | 是否必选 | 说明 |
---- | ---- | ---- | ---- |
cid | Number | 必选，仅适用于 V2 接口 | 房间 ID。该 ID 为创建房间接口调用成功后返回的房间 ID。仅在调用 V2 接口时需要设置。 |
taskId | String | 是 | 任务唯一标识符，房间内唯一。调用 [创建云端录制任务](https://doc.yunxin.163.com/nertc/server-apis/TEyOTk2OTY?platform=server) 接口后，可在响应体中获取 taskId。 |
recordConfig | [recordConfig](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#recordConfig) | 否 | 录制模式。详细信息请参考 [recordConfig](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#recordConfig)。<br> 支持如下三种模式：<ul> <li> **合流+单流录制（默认）**。房间内所有或指定 UID 的媒体流混合录制为一个媒体文件，同时每个 UID 均有其对应的媒体文件。<li> **合流录制**。房间内所有或指定 UID 的媒体流混合录制为一个媒体文件。<li> **单流录制**：分开录制房间内每个 UID 的媒体流，每个 UID 均有其对应的媒体文件。<br><note type="note">请在开始录制的时候设置录制模式，开始录制后，不支持切换录制模式。</note> |
streamSubscribe | [streamSubscribe](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#streamSubscribe) | 否 | 在录制过程中，通过黑白名单指定订阅某些用户的音频流或视频流。具体请参考 [订阅名单](https://doc.yunxin.163.com/nertc/server-apis/jQwNDAxNDc?platform=server)。<br>默认为订阅房间内所有发布的音视频流。 |
detect | [detect](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#detect) | 否 | 内容安全审核配置。详细信息请参考 [detect](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#detect)。 |
layoutConfig | [layoutConfig](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#layoutConfig) | 否 | 录制布局配置。详细信息请参考 [录制布局](https://doc.yunxin.163.com/nertc/server-apis/TA0MjE1MzE?platform=server)   <br> |\
| | | | 录制布局包括以下几种模式：|\
| | | | - 画廊模式 |\
| | | | - 主讲人模式 |\
| | | | - 自定义布局。 |\
| | | | - 悬浮布局。 |\
| | | | - 垂直布局。 |
watermark | [watermark](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#watermark) | 否 | 水印配置。详细信息请参考 [watermark](https://doc.yunxin.163.com/nertc/server-apis/jc2MjQzMzA#watermark)。<note type="note">该功能只针对合流录制模式的录制文件生效。</note> |

<!-- 
storageConfig | Object | 可选 | 云存储配置信息
  bucket | String | 必选 | 存储桶名称
  accessKey | String | 必选 | 访问密钥 ID
  secretKey | String | 必选 | 访问密钥密码
  region | String | 必选 | 存储厂商的存储桶可用区域，例如：`ap-southeast-1`
  storageProvider | Number | 必选 | 存储厂商类型，取值包括：<br>0："nos"(网易云)<br>1："ali"(阿里云OSS)<br>2："cos"(腾讯云COS)<br>3："s3"(AWS S3)<!-- <br>"blob"(Azure Blob Storage)<br>"gcs"(Google Cloud Storage) 
  filePrefix | String | 可选 | 对象存储前缀，例如，`"recordings/"`。
  endpoint | String | 可选 | 自定义存储端点 URL。
  encryption | Boolean | 可选 | 是否启用服务端加密。
  timeout | Number | 可选 | 上传超时时间，单位为秒。
  acl | String | 可选 | 访问控制列表设置，例如："public-read"。 -->

### 请求体示例

更新录制布局时，您只需要在更新录制布局接口请求体中修改需要更新的布局参数，其余参数保持原云端录制任务的设置（可通过 [查询云端录制配置](https://doc.yunxin.163.com/nertc/server-apis/TU4ODczNDk?platform=server) 接口查询）。

```JSON
{
    "cid":11***77184,
    "taskId":"23d73e2ae2c74135a732c8d24739b71d8723",
    "recordConfig": {
        "modeList": [
            {
                "mode": 100
            },
            {
                "layout": {
                    "canvas": {
                        "height": 720,
                        "width": 1280
                    },
                    "subStreams": [
                        {
                            "adaption": 0,
                            "height": 720,
                            "width": 1280,
                            "x": 0,
                            "y": 240,
                            "zOrder": 0
                        }
                    ],
                    "users": [
                        {
                            "adaption": 0,
                            "height": 240,
                            "width": 320,
                            "x": 0,
                            "y": 0,
                            "zOrder": 1
                        }
                    ]
                },
                "layoutType": 2,
                "mode": 1
            }
        ],
        "recordType": 100,
    },
    "detect":{
        "enableSpamDetect":true,
        "scFrequency":1,
        "detectType":1
    },
    "layoutConfig":{
        "hostUid":66602,
        "layoutType":3
    },
    "streamSubscribe":{
        "streamType":2,
        "audioUidList":{
            "unSubscribeUids":[
            66668888
            ]
        },
        "videoUidList":{
            "subscribeUids":[
            2223333
            ]
        }
    },
    "watermark":{
        "literaWms":[
            {
                "wmLitera": "水印测试",
                "fontSize":50,
                "fontColor":"#RRGGBB",
                "offsetX":0,
                "offsetY":0
            }
        ],
        "transparentLayers":[
            {
                "offsetX":0,
                "offsetY":0,
                "wmWidth":480,
                "wmHeight":100,
                "bgTransparency":3
            }
        ],
        "imgWms":[
            {
                "url":"https://freepngimg.com/XXX/XXX/example.png",
                "offsetX":120,
                "offsetY":120,
                "wmWidth":100,
                "wmHeight":100
            }
        ],
        "timestampWm":{
            "fontSize":50,
            "fontColor":"#RRGGBB",
            "offsetX":0,
            "offsetY":50
        }
    }
}
```

## 响应信息

### 响应体参数

返回结构请参考 [通用返回结构](https://doc.yunxin.163.com/nertc/server-apis/TYyNjA3MzQ?platform=server#%E5%93%8D%E5%BA%94%E7%BB%93%E6%9E%9C)。其中，业务相关参数封装在 `record` 参数中，包括：

| 参数名称 | 类型 | 示例 | 说明 |
| ---- | ---- | ---- | ---- |
| taskId | String | 1143932537695968 | 录制任务 ID，为任务唯一标识符，且房间内唯一。 |
| cid | Number | 123456 | 房间 ID。 |

### 响应体示例

```JSON
{
    "code": 200,
    "record": {
        "taskId": "23d73e2ae2c74135a732c8d24739b71d8723",
        "cid": 11443***77184
    }
}
```

### 状态码

响应内容中，code 为 200 表示调用正常，若 `code` 为其他值，请根据 `code` 与 `errmsg` 在 [云端录制相关错误码](https://doc.yunxin.163.com/nertc/server-apis/TQ3Njc1Nzg#云端录制相关错误码) 中查看问题原因与解决方法。