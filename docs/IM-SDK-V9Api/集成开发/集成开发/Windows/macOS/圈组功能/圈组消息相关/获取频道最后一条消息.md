<!--keywords: 最后一条消息, 频道最后一条消息, 最后一条 -->


网易云信 IM 的圈组模块支持在频道列表显示当前各频道的最后一条消息，且开发者可通过在应用上层自行开发相关业务逻辑，实现频道列表动态更新各频道的最后一条消息。

频道列表显示最后一条消息的简易 UI 示例如下：




![频道最后一条消息UI示例.png](https://yx-web-nosdn.netease.im/common/dc23e8ce693f220011fe3c28bb97b773/频道最后一条消息UI示例.png)


## 接口介绍

网易云信 NIM SDK 的[`Message`](https://doc.yunxin.163.com/messaging/references/pc/doxygen/Latest/zh/classnim_1_1_message.html)类提供[`GetLastMessages`](https://doc.yunxin.163.com/messaging/references/pc/doxygen/Latest/zh/classnim_1_1_message.html#a5137defd062158969365ec31a8c9324b)方法，用于获取多个频道的最后一条消息的初始数据。


该方法的响应[`QChatGetLastMessagesResp`](https://doc.yunxin.163.com/messaging/references/pc/doxygen/Latest/zh/structnim_1_1_q_chat_get_last_messages_resp.html)返回的结果为 map, key 值对应每一个`channel_id`，value 值是消息体[`QChatMessage`](https://doc.yunxin.163.com/messaging/references/pc/doxygen/Latest/zh/structnim_1_1_q_chat_message.html)。

## 前提条件

已登录圈组，并已创建圈组服务器和频道。


## 实现方法


### **实现流程**

1. 获取频道最后一条消息的初始数据。
    1. 调用[`GetChannelsByPage`](https://doc.yunxin.163.com/messaging/references/pc/doxygen/Latest/zh/classnim_1_1_channel.html#a310e32a34028a5c2a2559d5ced3849ad)拉取频道列表。
    2. 调用[`GetLastMessages`](https://doc.yunxin.163.com/messaging/references/pc/doxygen/Latest/zh/classnim_1_1_message.html#a5137defd062158969365ec31a8c9324b)方法获取若干个频道的最后一条消息。

      ::: note notice :::
      - 您需自行维护调用该方法返回的结果。
      - 被撤回的消息仍能通过调用该方法查到，但被删除的消息无法查到。如果最后一条消息是撤回消息，推荐把对应的最后一条消息置空，并给出提示表明“撤回消息”。
      :::

    3. 在您的应用内存中维护相关频道的最后一条消息。
2. 注册消息接收回调（[`RegRecvCb`](https://doc.yunxin.163.com/messaging/references/pc/doxygen/Latest/zh/classnim_1_1_message.html#aa4787c06597b0e6e9b6b31529bd1630d)）和消息更新回调（[`RegUpdatedCb`](https://doc.yunxin.163.com/messaging/references/pc/doxygen/Latest/zh/classnim_1_1_message.html#a0d47693a07a9eb59072054e71dbc46bf)），分别监听各频道的消息接收和消息更新。
3. 参照下表，在您的应用层**自行开发**，实现后续频道最后一条消息在不同场景下的动态更新。


    <div style="width:100px">场景</div> | 场景说明     |  推荐处理方法
    ---- | -------------- | ---------
    回调触发更新最后一条消息 | 频道内，其他用户发送消息或重发消息，触发消息接收回调| 更新频道最后一条消息<div></div>
    ^^ |   频道内，其他用户撤回、更新或删除消息，触发消息更新回调   |  判断撤回、更新、删除的消息是否为频道最后一条消息，若非最后一条消息，则忽略；若为最后一条消息，且：<div><ul><li>为撤回或删除消息，则把最后一条消息置空</li><li>为更新消息，则更新频道最后一条消息 </li></ul> </div>
    用户自己调用 API 后触发更新最后一条消息        |     自己在频道内发送、重发或更新消息      | 更新频道内最后一条消息
   ^^  | 自己在频道内撤回或删除消息| 判断撤回或删除的消息是否为频道最后一条消息，若非最后一条消息，则忽略；若为最后一条消息，则把最后一条消息置空
   
  
### **API调用时序示例**

下图以用户A、用户B 和用户C 的消息交互为例，为您展示实现频道最后一条消息动态更新的 API 调用时序，供您参考。



![频道最后一条消息(PC).png](https://yx-web-nosdn.netease.im/common/06970b81a71361d841037928d9fd5f73/频道最后一条消息(PC).png)


### **示例代码**


#### **获取初始数据**

```cpp
QChatGetLastMessagesParam param;
param.server_id = 123456;
param.channel_ids = {123456, 456789};
param.cb = [this](const QChatGetLastMessagesResp& resp) {
    if (resp.res_code != NIMResCode::kNIMResSuccess) {
        // error handling
        return;
    }
    // process response
    // ...
};
Message::GetLastMessages(param);
```

#### **回调触发更新最后一条消息**

```cpp
QChatRegMsgUpdatedCbParam reg_msg_updated_cb_param;
reg_msg_updated_cb_param.cb = [this](const QChatMsgUpdatedResp& resp) {
    if (resp.res_code != NIMResCode::kNIMResSuccess) {
        // error handling
        return;
    }
    // process response
    // ...
};
Message::RegUpdatedCb(reg_msg_updated_cb_param);
```


#### **用户自己调用API后触发更新最后一条消息**

具体示例代码请参见[圈组消息管理](https://doc.yunxin.163.com/docs/TM5MzM5Njk/DE4MDgzNDc?platformId=60227)

