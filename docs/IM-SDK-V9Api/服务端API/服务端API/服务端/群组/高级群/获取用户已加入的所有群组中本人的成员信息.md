查询指定用户已加入的所有群组中本人的成员信息。

## 使用限制

单个应用中一秒内所有的高级群操作 API 合计最多可调用 100 次，超过后限制调用，会返回 416 错误码。

:::note note
除发送群消息 API 外，其他所有高级群 API 都属于高级群操作 API。
:::

## 请求信息

### 请求 URL

```
POST https://api.yunxinapi.com/nimserver/team/listMemberInfo.action HTTP/1.1
Content-Type: application/x-www-form-urlencoded;charset=utf-8
```

### 请求头参数

POST 请求中 Headers 的设置请参考 [API 调用方式](/docs/TM5MzM5Njk/jk3MzY2MTI)。

### 请求体参数

<table>
    <thead>
        <tr> <th>参数</th><th>类型</th><th>必填</th><th>说明</th></tr></thead>
        <tr> <td>accid</td><td>String</td><td>是</td><td>需要查询的用户账号，accid，最大长度 32 位字符</td></tr>
</table>

### 请求示例

```cURL
curl -X POST -H "AppKey: go9dnk49***glw0803mgq3" -H "Nonce: 4tggger** t23t" -H "CurTime: 1443592222" -H "CheckSum: 9e9db3b6c9abb2e1962cf3e6f7316fcc55583f86" -H "Content-Type: application/x-www-form-urlencoded" -d 'accid=user12' 'https://api.yunxinapi.com/nimserver/team/listMemberInfo.action'
```

## 返回信息

### 返回参数

参数 | 类型 | 说明
:---- | :---- | :----
code | Integer | 状态码
data | String | 已加入的所有群组中的本人的成员信息
    tid | Long | 群 ID
    accid | String | 群成员账号 ID
    nick | String | 群昵称
    mute | Boolean | 是否禁言
    custom | String | 自定义扩展信息
    managerPushEnable | Boolean | 是否接收管理员消息推送
    pushEnable | Boolean | 是否接收推送
    createtime | Long | 创建时间
    updatetime | Long | 更新时间

### **成功返回示例**

```JSON
"Content-Type": "application/json; charset=utf-8"
{
    "code": 200,
    "data": [
        {
            "tid": 146684581,
            "accid": "u1204212",
            "nick": "",
            "mute": false,
            "managerPushEnable": false,
            "pushEnable": true,
            "custom": null,
            "createtime": 1609317189002,
            "updatetime": 1609317189002
        }
    ]

}
```

### 失败返回示例

```JSON
    "Content-Type": "application/json; charset=utf-8"
        {
            "code": 414, // 参数错误
            "desc": "xxxx is empty"
}
```

### 状态码

该接口在 HTTPS Body 中返回请求的状态码，以下仅列出与接口业务相关的状态码。完整状态码请参考 [状态码](https://doc.yunxin.163.com/messaging/docs/TM5NTk2Mzc?platform=server)。

状态码 | 说明 | 处理建议
:---- | :---- | :----
200 | 请求成功 | -
403 | 禁止操作 | -
414 | 参数错误 | 根据提示信息，检查传入参数的格式和限制条件
416 | 调用频率超出限制 | 降低访问频率
500 | 服务出错 | -