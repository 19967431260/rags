<!--keywords: 即时通讯，IM，圈组，频道，频道分组，服务器，API，示例-->

频道分组是对服务器下频道的分类管理。您可通过创建，删除，修改，查询等操作，实现对频道以及成员的精细化管理。




## 功能介绍 


频道分组根据查看模式分为**公开**和**私密**两种。

|  <div style="width:100px">查看模式 </div>  | 说明 |
|  ----  | -------  |
| 公开 | 除频道分组的黑名单成员和黑名单身份组成员以外，服务器中的其他成员都能看到公开频道分组。  |
| 私密 | 除频道分组的白名单成员和白名单身份组成员以外，服务器中的其他成员都无法看到私密频道分组。 |


::: note note 
被加入黑白名单的用户，即为**黑白名单成员**；被加入黑名单或白名单的身份组即为**黑白名单身份组**。
:::

<br>

圈组服务器中的成员都可以查询该服务器下的频道分组信息。

除查询频道分组信息外，其他频道分组的相关操作，都需要拥有满足两个前提条件。
- 能够查看到该频道分组信息。是否能够查看，与频道分组的查看模式和黑白名单相关，具体可以查看上述表格进行判断。
- 拥有管理频道权限。




## <span id='创建频道分组'>创建频道分组</span>

创建频道分组。

### URL 
```http
POST http://api.yunxinapi.com/nimserver/qchat/createChannelCategory.action  HTTP/1.1
Content-Type: application/x-www-form-urlencoded;charset=utf-8
```



### 请求参数

- POST 请求中 Headers 的设置请参考 [API 调用方式](https://doc.yunxin.163.com/messaging/server-apis/jk3MzY2MTI?platform=server)。

- POST 请求中 Body 的设置如下：

<table>
    <thead><tr> <th>参数</th><th>类型</th><th>必须</th><th>说明</th></tr></thead>
        <tr> <td>accid</td><td>String</td><td>是</td><td>操作者accid</td></tr>
        <tr> <td>serverId</td><td>String</td><td>是</td><td>服务器唯一标识</td></tr>
        <tr> <td>name</td><td>String</td><td>否</td><td>名称，最大长度64字符</td></tr>
        <tr> <td>custom</td><td>String</td><td>否</td><td>自定义扩展，最大长度1024字符</td></tr>
        <tr> <td>viewMode</td><td>String</td><td>否</td><td>查看模式：0-公开(默认)，1-私密。</td></tr>
        <tr> <td>reorderweight</td><td>Long</td><td>否</td><td>频道分组自定义排序权重值，默认为 0。<note type=note>该参数需配合 SDK 支持圈组功能的各端的排序规则参数生效。如排序规则字段为默认倒序，则匹配的频道分组列表以权重值倒序排列。</note></td></tr>
</table>

### 返回参数

参数 | 说明
:-----|:-----
categoryInfo | 频道分组信息，具体见下表

categoryInfo 参数如下：

参数| 类型| 说明
:-----|:--|:---
categoryId | Long | 频道分组 ID
serverId| Long  | 服务器 ID 
name | String  | 频道分组名称
custom | String | 频道分组自定义扩展字段
owner |  String | 服务器所有者
viewMode | Integer | 频道的查看模式，0-公开模式(默认)，1-私密模式
channelNumber | Integer | 频道数量
validFlag | Integer | 有效标志：0-无效，1-有效
createTime | Long | 创建时间
updateTime  | Long  | 更新时间


### 示例

#### cURL 请求示例
```curl
curl -X POST -H "AppKey: go9dnk***0803mgq3" -H "Nonce: 4tggger**t23t" -H "CurTime: 1443592222" -H "CheckSum: 9e9db3b6c9abb2e1962cf3e6f7316fcc55583f86" -H "Content-Type: application/x-www-form-urlencoded" -d 'accid=zhangsan&serverId=1513535' 'http://api.yunxinapi.com/nimserver/qchat/createChannelCategory.action'
```

#### 返回示例
```json
"Content-Type": "application/json; charset=utf-8"
{
    "code":200,
    "categoryInfo":{
        "categoryId": 123,
        "serverId": 456,
        "name": "name",
        "custom": "custom",
        "owner": "zhangsan",
        "viewMode": 0,
        "channelNumber": 10,
        "validFlag": 1,
        "createTime": 1655197548301,
        "updateTime": 1655197548301
    }
}
```

### 状态码

该接口在 HTTPS Body 中返回请求的状态码，以下仅列出与接口业务相关的状态码。完整状态码请参见 [状态码](https://doc.yunxin.163.com/messaging/server-apis/TM5NTk2Mzc?platform=server)。

200、403、414、416、431、500
