本文主要介绍服务端的常见问题以及解决方法。

## 重发消息为什么不触发第三方回调？

在同时使用第三方回调和消息重发的场景下，如果第三方服务器返回是：200（错误码）+deny 这种形式，云信服务器默认会缓存该结果，之后重发消息将不会再触发第三方回调。

您可以通过以下两种方法解决：

- 自行调整返回的错误信息，调整成非 200 （错误码）+deny 的形式，云信服务端就不会进行缓存，重发消息仍走第三方回调。

- 联系云信技术支持，申请兼容 200（错误码）+deny 形式。云信服务端会修改逻辑，之后收到该形式的错误信息后不进行缓存，重发消息仍走第三方回调。