<!--keywords: 导入组件,UIKit,IM UIKit,IMUIKit,Kit -->

您可以采用添加远端仓库依赖的方式，将所需组件快速导入到您的项目中。

## 准备工作

根据本文操作前，请确保您已经完成了以下设置：

- 已在 [网易云信控制台](https://app.yunxin.163.com/global/home) [创建应用](https://doc.yunxin.163.com/console/docs/TIzMDE4NTA?platform=console)，获取 App Key。
- 已 [注册 IM 账号](https://doc.yunxin.163.com/messaging-uikit/guide/TUwNTQ2NjM?platform=flutter#4-注册-im-账号)，获取账号 ID（`account_id`）和凭证（Token）。
- 准备如下开发环境/工具：
    - Flutter-dart 3.0.0 及以上版本。
    - 安卓：
        - Android Studio Bumblebee
        - App 要求安卓 5.0（API 21）及以上版本设备
        - 1.6.10 以上版本的 `kotlin-gradle-plugin`
    - iOS：
        - Xcode 11.0 及以上版本
        - App 要求 iOS 11.0 以上版本设备
        - 请确保您的项目已设置有效的开发者签名

## 添加依赖

1. 在您项目的 `pubspec.yaml` 中按需添加依赖。例如，如果您只需集成聊天界面，可以添加 `nim_chatkit_ui` 依赖。

    - 远程依赖：

        ```YAML
        dependencies:
            # 通讯录功能
            nim_contactkit_ui: ^1.0.0
            # 会话列表功能组件
            nim_conversationkit_ui: ^1.0.0
            # 群组功能组件
            nim_teamkit_ui: ^1.0.0
            # 聊天功能组件
            nim_chatkit_ui: ^1.0.0
            # 搜索功能组件
            nim_searchkit_ui: ^1.0.0
        ```

        ::: note note
        上述示例代码中的 `1.0.0` 表示版本号，请根据实际情况进行替换。
        :::

    - 本地依赖：

        ```YAML
        dependencies:
            # 通讯录功能
            nim_contactkit_ui:
                path: ../nim_contactkit_ui
            # 会话列表功能组件
            nim_conversationkit_ui:
                path: ../nim_conversationkit_ui
            # 群组功能组件
            nim_teamkit_ui:
                path: ../nim_teamkit_ui
            # 聊天功能组件
            nim_chatkit_ui:
                path: ../nim_chatkit_ui
            # 搜索功能组件
            nim_searchkit_ui:
                path: ../nim_searchkit_ui
        ```

        ::: note note
        上述示例代码中的 `path` 路径都是相对路径，请根据实际情况进行替换。
        :::

2. 执行 `flutter pub get` 命令更新依赖。