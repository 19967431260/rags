通讯录组件（`ContactListContainer`）主要功能为显示当前用户的好友、黑名单以及群组关系。

## 界面效果

通讯录组件由以下个子组件组成：

- `ContactListContainer`：关系导航组件。
- `ContactInfoContainer`：关系详情组件，内部包含了好友列表、群组列表以及黑名单列表。

    <img alt="Web 通讯录界面 930.png" src="https://yx-web-nosdn.netease.im/common/873aaf2964c33fabbe4d5e0d093ff98c/Web通讯录界面930.png" style="width:80%;border: 1px solid #BFBFBF;">

## 界面集成

在您的 React 或 Vue 项目中导入关系导航组件和关系详情组件，并配置界面相关设置（宽高、宽高比、位置），即可集成 IM UIKit 默认的通讯录界面。

示例代码如下：

:::::: div linked-codes
::: code React
```JavaScript
import React from 'react'
import { ContactListContainer, ContactInfoContainer } from '@xkit-yx/im-kit-ui'

import '@xkit-yx/im-kit-ui/es/style'

const Page1 = () => {
  return (
    <div
      style={{
        display: 'flex',
        width: 1000,
        height: 400,
        alignItems: 'center',
      }}
    >
      <div style={{ width: 300, height: '100%' }}>
        <ContactListContainer />
      </div>
      <div style={{ flex: 1, height: '100%', overflowY: 'auto' }}>
        <ContactInfoContainer />
      </div>
    </div>
  )
}
```
:::
::: code Vue
```JSX
<template>
   <div ref="contactList" />
   <div ref="contactInfo" />
</template>
<script>
import {
  ContactListContainer, // 通讯录——通讯录导航组件
  ContactInfoContainer, // 通讯录——通讯录详情组件，包含好友列表、群组列表以及黑名单列表
} from "@xkit-yx/im-kit-ui";
import "@xkit-yx/im-kit-ui/es/style/css";
...
  mounted() {
    this.$uikit.render(ContactListContainer, null, this.$refs.contactList);
    this.$uikit.render(
      ContactInfoContainer,
      {
        afterSendMsgClick: () => {
          this.model = "chat";
        },
        onGroupItemClick: () => {
          this.model = "chat";
        },
      },
      this.$refs.contactInfo
    );
  }
...
</script>
```
:::
::::::

## 定制界面

IM UIKit 默认的通讯录界面，支持如下功能：

- 单击用户头像查看用户资料卡片。
- 在用户资料卡片页面中拉黑，或者解除拉黑好友。

如果默认界面无法满足您的业务需求，请根据自定义参数进行自定义。通讯录组件（`ContactListContainer`）提供的自定义参数，请参考 [定制通讯录界面](https://doc.yunxin.163.com/messaging-uikit/guide/DIxNDE0Njk?platform=web)。

<!-- ### 关系导航

<div style="width:180px">参数</div> | 类型 | 说明 |
:---- | :---- | :--- |
`renderCustomContact` | `((contactType: ContactType) => Element)` | 自定义渲染通讯录内容，具体示例参考 [自定义渲染通讯录内容](https://doc.yunxin.163.com/messaging-uikit/guide/DA5MDQ5MDU?platform=web#自定义通讯录内容)。 |
`prefix` | String | 样式前缀，默认值为"contact"。 |
`onItemClick` | `((contactType: ContactType) => void)` | 通讯录导航单击事件。 |

### 关系详情

<div style="width:180px">参数</div> | 类型 | 说明 |
:---- | :---- | :--- |
`renderFriendListEmpty` | `(() => Element)` | 自定义渲染好友列表为空时的内容。 |
`renderFriendListHeader` | `(() => Element)` | 自定义渲染好友列表头部内容，具体示例参考 [自定义好友列表头部内容](https://doc.yunxin.163.com/messaging-uikit/guide/DA5MDQ5MDU?platform=web#自定义好友列表头部内容)。 |
`renderBlackListEmpty` | `(() => Element)` | 自定义渲染[黑名单列表为空时的内容](https://doc.yunxin.163.com/messaging-uikit/guide/DA5MDQ5MDU?platform=web#黑名单列表为空时的内容)。 |
`renderBlackListHeader` | `(() => Element)` | 自定义渲染[黑名单列表头部内容](https://doc.yunxin.163.com/messaging-uikit/guide/DA5MDQ5MDU?platform=web#黑名单列表头部内容)。 |
`renderGroupListEmpty` | `(() => Element)` | 自定义渲染[群组列表为空时的内容](#群组列表为空时的内容)。 |
`renderGroupListHeader` | `(() => Element)` | 自定义渲染[群组列表头部内容](https://doc.yunxin.163.com/messaging-uikit/guide/DA5MDQ5MDU?platform=web#群组列表为空时的内容)。 |
`renderMsgListEmpty` | `(() => Element)` | 自定义渲染[消息中心为空时的内容](https://doc.yunxin.163.com/messaging-uikit/guide/DA5MDQ5MDU?platform=web#消息中心为空时的内容)。 |
`renderMsgListHeader` | `(() => Element)` | 自定义渲染[消息中心头部内容](https://doc.yunxin.163.com/messaging-uikit/guide/DA5MDQ5MDU?platform=web#消息中心头部内容)。 |
`renderEmpty` | `(() => Element)` | 自定义渲染[通讯录为空时的内容](https://doc.yunxin.163.com/messaging-uikit/guide/DA5MDQ5MDU?platform=web#通讯录为空时的内容)。 |
`renderAIListEmpty` | `() => Element` | 自定义渲染数字人列表为空时内容。 |
`renderAIListHeader` | `() => Element` | 自定义渲染数字人列表头部内容。 |
`afterSendMsgClick` | `(() => void)` | 单击发送消息后的事件，例如在同意好友申请后，可自定义渲染 [打招呼消息](https://doc.yunxin.163.com/messaging-uikit/guide/DA5MDQ5MDU?platform=web#自定义打招呼消息)。 |
`afterAcceptApplyTeam` | `(actionInfo: V2NIMTeamJoinActionInfoForUI) => void` | 通过入群申请后的事件。 |
`afterRejectApplyTeam` | `(actionInfo: V2NIMTeamJoinActionInfoForUI) => void` | 拒绝入群申请后的事件。 |
`afterAcceptTeamInvite` | `(actionInfo: V2NIMTeamJoinActionInfoForUI) => void` | 通过入群邀请后的事件。 |
`afterAcceptTeamInvite` | `(actionInfo: V2NIMTeamJoinActionInfoForUI) => void` | 拒绝入群邀请后的事件。 |
`afterAcceptApplyFriend` | `(application: V2NIMFriendAddApplicationForUI) => void` | 通过好友申请后的事件。 |
`afterRejectApplyFriend` | `(application: V2NIMFriendAddApplicationForUI) => void` | 拒绝好友申请后的事件。 |
`onFriendItemClick` | `((account: string) => void)` | 单击好友的事件。 |
`onGroupItemClick` | `((team: Team) => void)` | 群组单击事件。 |
`onBlackItemClick` | `((account: string) => void)` | 黑名单单击事件。 |
`onAIItemClick` | `(aiUser: V2NIMAIUser)` => void | AI 数字人单击事件。 |
`commonPrefix` | String | 公共样式前缀，默认值：`"common"`。 | -->