<!--keywords: 声纹查询, 语音识别, 智能体平台, 声纹向量, 用户声纹-->

根据用户 ID 查询该用户在网易云信智能体平台上已添加的所有声纹信息，可用于声纹管理和语音识别业务场景。

## 请求信息

### 请求 URI

```
GET https://rtc-agent.yunxinapi.com/v1/voice_prints
```

<style>
table th:first-of-type {
    width: 20%;
}
</style>

### 请求头参数

请求 Header 的参数说明请参考 [请求结构](https://doc.yunxin.163.com/ai-hardware/server-apis/DYwNTk5MzM?platform=server#Header)。

### 请求查询参数

| 参数名称 | 类型 | 是否必选 | 示例 | 说明 |
| :---- | :---- | :---- | :---- | :---- |
| uid | String | 是 | user1 | 用户唯一标识，用于查询该用户的声纹信息 |
| subAccountUid | String | 否 | subAccountUid | 子用户 ID，在操作子用户时候必填。最大长度 32 字节，只允许字母、数字以及 "_@.-" |


### 请求体示例

```
curl https://rtc-agent.yunxinapi.com/v1/voice_prints?uid=user1
```

## 响应信息

### 响应参数

| 参数名称 | 类型 | 说明 | 是否必返回 |
| :---- | :---- | :---- | :---- |
| code | Integer | 状态码，200 表示请求成功 | 是 |
| message | String | 提示信息。请求失败时返回错误信息，请求成功时返回 "success" | 是 |
| data | Object | 返回的 JSON 数据对象，请求失败则返回空对象 | 是 |
    voicePrints | Array | 声纹列表，包含用户的所有声纹信息 | 是 |
      name | String | 声纹名称，用户自定义的声纹标识 | 是 |
      embedding | Array | 声纹向量数据，用于声纹识别计算 | 是 |
      voicePrintId | String | 声纹唯一标识符 | 是 |

### 响应体示例

```JSON
{
    "code": 200,
    "message": "success",
    "data": {
        "voicePrints": [
            {
                "name": "jack",
                "embedding": [0.0067223333333, -0.23333344433],
                "voicePrintId": "99IOEqmdsa"
            },
            {
                "name": "tom",
                "embedding": [-0.54221334241, 0.4124222987],
                "voicePrintId": "99IOEqmdl5u"
            }
        ]
    }
}
```

<!-- ### 错误码

本文仅列举部分业务接口错误码，完整列表请参考 [错误码](https://doc.yunxin.163.com/ai-hardware/server-apis/DY5MDEzOTQ?platform=server)。

| 错误码 | 说明 | 处理建议 |
| :---- | :---- | :---- |
| 400 | 请求参数错误 | 检查 uid 参数是否正确传递 |
| 401 | 身份验证失败 | 检查请求头中的认证信息 |
| 404 | 用户不存在或无声纹数据 | 确认用户 ID 正确，或先添加声纹数据 |
| 500 | 服务器内部错误 | 欢迎 [提交工单](https://app.yunxin.163.com/global/service/ticket/create) 联系网易云信技术支持工程师 | -->