---
track_type: 排查类
sub_type: 使用问题
product: 呼叫组件
feature: 自定义消息
platform: Android
title: 呼叫组件发起通话携带自定义消息
root_cause: 配置了信令扩展字段，但未监听对应的信令抄送(eventType 31)
solution: 配置extras字段后，需要监听eventType 31的独立信令抄送，通过type字段过滤所需信令。
customers: ["VIP云信-长沙点盐科技有限公司-dx"]
source: chat_history
tags: ["呼叫组件", "自定义消息", "extras", "信令抄送"]
created: 2025-11-05
updated: 2026-03-20
---

## 问题：Android 呼叫组件发起通话携带自定义消息

## 问题详情

**现象**：
使用UIKit发起通话时，希望在创建房间或主播进入房间时携带自定义消息（如业务系统用户ID）。

**复现步骤**：
（从会话提取；无明确步骤则省略此节）

**环境信息**：
- SDK 版本：（从会话提取，无则省略）
- 系统版本 / 设备：（从会话提取，无则省略）
- 其他：（网络环境、账号类型等，无则省略）

**相关日志**：
（从会话提取关键报错日志，无则省略）

## 排查过程

1. 尝试使用extras字段 → 未在回调中显示
2. 确认抄送类型 → 应使用信令抄送(eventType 31)
**关键发现**：信令扩展字段需要通过信令抄送获取

**关键发现**：

## 问题原因

配置了信令扩展字段，但未监听对应的信令抄送(eventType 31)

## 解决方案

配置extras字段后，需要监听eventType 31的独立信令抄送，通过type字段过滤所需信令。

## 其他触发场景

