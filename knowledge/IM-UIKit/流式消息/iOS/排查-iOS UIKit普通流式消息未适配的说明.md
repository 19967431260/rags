---
track_type: 排查类
sub_type: 使用问题
product: IM-UIKit
feature: 流式消息
platform: iOS
title: iOS UIKit普通流式消息未适配的说明
root_cause: iOS UIKit尚未适配普通流式消息（由服务端通过流式消息接口发送的非AI对话流式消息）
solution: iOS UIKit普通流式消息暂未适配，预计在下一个版本（9月5日）发布后支持
customers: ["武汉盛游"]
source: chat_history
tags: ["流式消息","iOS","UIKit","10.8.4","消息刷新"]
created: 2025-08-25
updated: 2026-03-26
---

## 问题：iOS iOS UIKit普通流式消息未适配的说明

## 问题详情

**现象**：
iOS端使用UIKit（10.8.4版本），流式消息收到几个字就不刷新了，安卓正常。

**复现步骤**：
（无明确步骤，从会话提取）

**环境信息**：
- SDK 版本：10.8.4
- 系统版本 / 设备：iOS
- 其他：（网络环境、账号类型等，无则省略）

**相关日志**：
（从会话提取关键报错日志，无则省略）

## 排查过程

（无详细排查步骤，从会话提取）

**关键发现**：iOS UIKit尚未适配普通流式消息（由服务端通过流式消息接口发送的非AI对话流式消息）

## 问题原因

iOS UIKit尚未适配普通流式消息（由服务端通过流式消息接口发送的非AI对话流式消息）。

## 解决方案

iOS UIKit普通流式消息暂未适配，预计在下一个版本（9月5日）发布后支持。

## 其他触发场景

（合并时在此处追加）
