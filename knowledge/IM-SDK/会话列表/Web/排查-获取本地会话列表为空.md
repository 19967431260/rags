---
track_type: 排查类
sub_type: 配置开通咨询
product: IM-SDK
feature: 会话列表
platform: Web
title: 获取本地会话列表为空
root_cause: 管理后台消息漫游开关未开启，导致无法获取会话列表
solution: 在管理后台开启消息漫游功能，然后重新发送消息测试。
customers: ["VIP云信-银川鲸麦互联网医院有限公司"]
source: chat_history
tags: ["会话列表", "漫游", "getConversationList", "403"]
created: 2025-11-26
updated: 2026-03-20
---

## 问题：Web 获取本地会话列表为空

## 问题详情

**现象**：
调用getConversationList获取本地会话列表返回为空，但确定有一条会话数据。同时出现v2AIGetUserList 403错误。

**复现步骤**：
（从会话提取；无明确步骤则省略此节）

**环境信息**：
- SDK 版本：（从会话提取，无则省略）
- 系统版本 / 设备：（从会话提取，无则省略）
- 其他：（网络环境、账号类型等，无则省略）

**相关日志**：
（从会话提取关键报错日志，无则省略）

## 排查过程

1. 检查错误日志 → v2AIGetUserList 403 forbidden
2. 确认问题范围 → 本地会话列表为空
3. 分析初始化日志 → 同步已完成
4. 确认漫游设置 → 消息漫游开关未开启
**关键发现**：消息漫游开关未开启导致无法获取会话

**关键发现**：

## 问题原因

管理后台消息漫游开关未开启，导致无法获取会话列表

## 解决方案

在管理后台开启消息漫游功能，然后重新发送消息测试。

## 其他触发场景

