---
track_type: 排查类
sub_type: 使用问题
product: IM-SDK
feature: 群组管理
platform: iOS
title: 群成员禁言方法返回404错误
root_cause: 调用禁言接口时传入的userId不是该群的成员
solution: 确保传入的userId是群成员，可通过teamMember方法获取成员信息确认。
customers: ["VIP云信-重庆英纳华科技有限公司"]
source: chat_history
tags: ["禁言", "404", "updateMuteState", "群成员"]
created: 2025-11-21
updated: 2026-03-20
---

## 问题：iOS 群成员禁言方法返回404错误

## 问题详情

**现象**：
调用updateMuteState:userId:inTeam:completion:方法禁言群成员时返回Code=404，提示请求的目标不存在。

**复现步骤**：
（从会话提取；无明确步骤则省略此节）

**环境信息**：
- SDK 版本：（从会话提取，无则省略）
- 系统版本 / 设备：（从会话提取，无则省略）
- 其他：（网络环境、账号类型等，无则省略）

**相关日志**：
（从会话提取关键报错日志，无则省略）

## 排查过程

1. 确认SDK版本 → V9
2. 检查调用方式 → 使用底层SDK方法
3. 确认错误信息 → 404目标不存在
4. 排查userId → 传入的userId不是群成员
**关键发现**：传入的userId不在群内导致404

**关键发现**：

## 问题原因

调用禁言接口时传入的userId不是该群的成员

## 解决方案

确保传入的userId是群成员，可通过teamMember方法获取成员信息确认。

## 其他触发场景

