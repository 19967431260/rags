---
track_type: 排查类
sub_type: 使用问题
product: IM-SDK
feature: 历史消息查询
platform: 通用
title: 查询历史消息返回空数组
root_cause: 查询历史消息返回空数组可能原因：时间范围错误、账号未加入群聊、接口参数不正确
solution: 确认时间范围（beginTime小于endTime）、确认账号已加入群聊、检查接口参数是否正确；可使用会话消息历史接口替代。
customers: ["卫宁互联网科技有限公司"]
source: chat_history
tags: ["历史消息", "查询", "空数组", "时间范围"]
created: 2025-08-05
updated: 2026-03-26
---

## 问题：通用 查询历史消息返回空数组

## 问题详情

**现象**：
客户查询历史消息返回空数组。

**复现步骤**：
（从会话提取；无明确步骤则省略此节）

**环境信息**：
- SDK 版本：（从会话提取，无则省略）
- 系统版本 / 设备：（从会话提取，无则省略）
- 其他：（网络环境、账号类型等，无则省略）

**相关日志**：
（从会话提取关键报错日志，无则省略）

## 排查过程

1. 确认时间范围是否正确 → 检查beginTime和endTime
2. 确认账号是否已加入群聊 → 确认成员状态
3. 检查接口参数是否正确 → 确认参数设置

**关键发现**：查询历史消息返回空数组可能原因：时间范围错误、账号未加入群聊、接口参数不正确

## 问题原因

查询历史消息返回空数组可能原因：时间范围错误、账号未加入群聊、接口参数不正确

## 解决方案

确认时间范围（beginTime小于endTime）、确认账号已加入群聊、检查接口参数是否正确；可使用会话消息历史接口替代。

## 其他触发场景

（合并时在此处追加：`- [{platform}] {title}，来源：{customers}，{today}`）
