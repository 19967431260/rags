---
track_type: 排查类
sub_type: 配置开通咨询
product: IM-SDK
feature: 会话置顶
platform: 通用
title: 置顶接口返回403错误
root_cause: 使用的是云端会话，但云端会话功能未开通。
solution: 1. 开通云端会话功能（高级版）；2. 如果使用本地会话，需要用localConversationService调用置顶接口。
customers: ["VIP云信-四川斯特雅科技有限责任公司"]
source: chat_history
tags: ["置顶", "403", "云端会话", "localConversationService"]
created: 2025-11-04
updated: 2026-03-20
---

## 问题：通用 置顶接口返回403错误

## 问题详情

**现象**：
已开启全局置顶功能，但调用置顶接口返回403错误。

**复现步骤**：
（从会话提取；无明确步骤则省略此节）

**环境信息**：
- SDK 版本：（从会话提取，无则省略）
- 系统版本 / 设备：（从会话提取，无则省略）
- 其他：（网络环境、账号类型等，无则省略）

**相关日志**：
（从会话提取关键报错日志，无则省略）

## 排查过程

1. 检查SDK日志确认使用的appkey；2. 确认使用的是云端会话还是本地会话。

**关键发现**：

## 问题原因

使用的是云端会话，但云端会话功能未开通。

## 解决方案

1. 开通云端会话功能（高级版）；2. 如果使用本地会话，需要用localConversationService调用置顶接口。

## 其他触发场景

